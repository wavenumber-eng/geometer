+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T233443-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T23:34:43-04:00"
+++

# Log: ordered Boolean stage selection and lineage feasibility

Exact classified faces now feed a deterministic ordered-stage evaluator.
Stages retain authored order; operands within each union or difference stage
sort by complete coverage/source identity and reject duplicate identities.
Each bounded coverage is declared exactly once, and every classified coverage
must belong to the stage program before evaluation.

Union marks every covered face present and adds every covered positive source,
including absorbed and coincident positives when material was already present.
Difference acts only on currently present cells, clears their positive lineage,
and retains every covered subtractive source as effect history. A later union
into a removed cell starts fresh positive lineage without erasing the earlier
subtraction event. Empty/uncovered stage work is a no-op. All source lists are
unique, sorted, and independent of operand input order.

The closed-form vector uses four nested/coincident rectangular coverages. It
proves two absorbed positives survive together, a nested difference produces
an absent cell with subtraction history, and a later inner refill contains
only its new positive source while retaining that history. Operand permutation
produces identical bytes. Duplicate coverage metadata, a one-unit-short work
budget, and a one-byte-short storage budget fail closed without publication or
logical-storage leakage.

The native/Emscripten canonical signature SHA-256 is
`2fb0a89e6226f01bddde8cdb920ec3e2c22f9f3f09b3681fe0f884d64c33a8a5`.
The exact evaluator boundary is 416 work units and 4,560 conservatively
reserved logical bytes.

This is stage-selection and cell-lineage feasibility, not a production
operation. Result-region merging and many-to-many associations, boundary and
vertex provenance projection, final normalization/packet encoding, OCCT audit,
broader synthetic/property/mutation coverage, and dispatch remain pending;
`production_solver_allowed` remains false.

Validation passes: native CTest 23/23; governed exact Emscripten CTest 14/14;
stage-selection native/WASM parity; promotion-manifest tests 13/13; full Rack
77 passed with only the intentional active-plan L99 sentinel failing; Ruff;
Pyright; uv lock; clang-format; Lizard; and wn-dev-std 2026.8.12 repository,
docs.plans, and language audits. The maintained browser-WASM Rack stratum also
passes 3/3.
