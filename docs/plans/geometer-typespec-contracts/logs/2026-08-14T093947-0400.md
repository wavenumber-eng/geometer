+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T093947-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T09:39:47-04:00"
+++

# Log: exact moved-arc result-normalization certification

Final result normalization now certifies circular-arc replay against the
governed symmetric 1.25 nm Hausdorff limit. The exact predicate partitions a
source arc by the target arc's closest-feature transitions and checks the
finite extrema set: source endpoints, circle-center radial extrema, endpoint-
distance extrema, radial-domain boundaries, and the endpoint Voronoi boundary.
It uses real-algebraic constructions and exact comparisons only; there is no
trigonometry, floating-point arithmetic, sampling, or OCCT geometry decision.

Congruent translated arcs have an exact fast path: equal radius, branch, and
direction plus identical center/start/end translations reduce the Hausdorff
decision to the squared translation length. This both proves the common
normalization case directly and avoids unnecessary high-degree constructions.
All paths retain bounded work/storage accounting, exception containment, and
fail closed on invalid or resource-limited inputs.

The closed-form predicate checks cover identical minor and major arcs,
concentric arcs exactly on and outside their radial-distance bound, translated
arcs exactly on the 1.25 nm boundary and outside a 1.2 nm bound, and invalid
thresholds. One-unit-short work and one-byte-short storage boundaries fail
without retaining storage. The normalization vector now accepts a two-half-
arc circle whose center and endpoints move by one quarter nanometer, then
rebuilds the rounded fragments through arrangement, ordered stages, and result
regions. Its native signature SHA-256 is
`db3be0a6e7bfc88bde2435bc603569d9dac18d8d320a32e2d0e48a8e00e469ea`.

This remains feasibility infrastructure. Complete result-packet encoding,
source-set interning, OCCT qualification, production runtime integration, and
the broader synthetic/mutation program remain pending.
`production_solver_allowed` remains false.

Validation passes: native CTest 27/27; native/Emscripten governed-vector
parity; promotion-manifest tests 13/13; Ruff; Pyright; uv lock; clang-format;
Lizard; and wn-dev-std 2026.8.12 repository, docs.plans, and language audits.
Full Rack reports 77 passed with only the intentional active-plan L99 sentinel
failing.
