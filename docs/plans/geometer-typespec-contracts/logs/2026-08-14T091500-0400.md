+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T091500-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T09:15:00-04:00"
+++

# Log: certified grid-exact result normalization topology

The accepted exact arrangement, selection, region, provenance, and operand-
outcome pipeline now has its first endpoint-authoritative final-result
normalization projection. Every retained vertex is rounded once with the
certified ties-away-from-zero scalar normalizer and must remain within the
governed squared displacement of 0.5 nm2. Distinct required vertices mapping
to one integer-nm coordinate fail with the stable topology-collapse outcome.

The projector validates complete material-boundary/ring closure and ring/
region hierarchy before use. It reconstructs integer-nm line and circular-arc
fragments, then feeds those replay fragments back through the exact arrangement,
coverage, ordered-stage, and result-region builders. The replay must preserve
every fragment, vertex, ring, result region, parent/depth link, winding, and
material ownership. Crossings, merges, cyclic-order changes, containment
changes, or fragment reversal therefore fail closed rather than becoming a
published normalized topology.

Circular fragments currently pass this slice only when endpoints and radius
are already exact integer-nm values. Their center is nevertheless derived from
the published endpoints, radius, direction, and major branch and proven equal
to the source circle; two coherent half arcs replay a full circle without a
stored center. General moved-arc certification against the 1.25 nm Hausdorff
bound remains deliberately pending and is not approximated or accepted by this
slice. It is required before final result-packet promotion.

Closed-form vectors cover a successful empty result, an integer rectangle, an
endpoint-authoritative two-half-arc circle, an uncertified moved-arc rejection,
and the normative 1.25/1.4 nm collision pattern. A one-unit-short work boundary
fails without retaining normalization storage. Native and Emscripten produce
canonical signature SHA-256
`bb41b54ebd52dba0407c245c3d4cbd24c4ba2b28c7fc5a07a0df4603f67ffe58`.

This is feasibility infrastructure only. Source-set interning, complete packet
encoding/decoding, moved-arc Hausdorff certification, OCCT qualification, and
production dispatch remain pending; `production_solver_allowed` remains false.

Validation passes: native CTest 27/27; governed native/Emscripten parity;
promotion-manifest tests 13/13; Ruff; Pyright; uv lock; clang-format; Lizard;
and wn-dev-std 2026.8.12 repository, docs.plans, and language audits. Full Rack
reports 77 passed with only the intentional active-plan L99 sentinel failing.
