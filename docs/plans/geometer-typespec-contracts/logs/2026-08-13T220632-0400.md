+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T220632-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T22:06:32-04:00"
+++

# Log: exact geometry allocation-containment remediation

Independent review of `e76c7b2f989b26d7b8a7a4097efe171eb9ffaa89`
accepted the exact intersection formulas, classifications, transaction
rollback for returned errors, certified nm normalization, governed artifact,
and native/Emscripten parity. It found one blocking total-outcome defect:
binary `sum` and `product` calls constructed an initializer-list vector before
the arena could reserve storage or catch allocation failure, and the three
public intersection functions lacked outer exception containment.

The arena now provides governed binary sum/product entry points. They reserve
storage and consume a fixed allocation phase before materializing the
two-child vector, contain allocation or estimate exceptions, and then enter
the existing associative normalization transaction. The geometry builder uses
only those binary entry points. A deterministic one-unit-short preflight test
proves no child vector or semantic node is published when that phase cannot
start.

Each public line/line, line/circle, and circle/circle API now contains the
complete implementation behind a `std::exception` boundary. The internal
construction transaction unwinds first, releasing nodes and owned storage;
the public result is then `resource_limit_exceeded`. A source regression
protects the absence of initializer-list binary calls and the presence of all
three containment boundaries.

The added 32-unit binary phase changes the governed successful secant work
boundary to 41,482,728. Construction keys and the 1,248-byte artifact remain
unchanged, so its SHA-256 remains
`0af1965a293c1b7a90ec633d35ee98f2d53098350e86e922e8c18751ea15a5e0`.
Production solver dispatch remains disabled.

Validation at the fixed remediation tree: governed native validation and
CTest 20/20; all ten exact Emscripten CTests; parity validator at the unchanged
1,248-byte SHA and revised 41,482,728-unit boundary; promotion-manifest tests
13/13; Ruff; Pyright; clang-format; and wn-dev-std 2026.8.12 repository,
docs.plans, and language audits pass. Rack reports 72 passing tests with only
the intentional active-plan L99 hygiene sentinel failing.
