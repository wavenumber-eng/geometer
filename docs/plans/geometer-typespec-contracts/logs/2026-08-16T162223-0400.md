+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-16T162223-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-16T16:22:23-04:00"
+++

# Log: filtered-analytic-solver-design

# Validate replay column identities

Closed the remaining filtered-normalization proof-token trust boundary without
weakening strict replay or enabling the algebraic backend. Overlay now
revalidates every endpoint/cardinal-side token issued to endpoint-authoritative
arcs against a dense canonical group table ordered by cardinal side and exact
integer endpoint. Zero-payload tokens, skipped groups, split equal endpoint
keys, and reuse of one group by unrelated endpoints fail as invalid input
before the face sweep can consume the correlation proof.

The validator reuses the already governed two-actions-per-curve overlay scratch
and bulk-charges a fixed five work units per curve. It performs one curve scan
and one dense group scan, with no pair search or target-dependent allocation.
Its binding, keying, and sequence responsibilities are separate functions; the
new functions are at most 31 NLOC and CCN 11. The arrangement admission bound
now includes this mandatory downstream work, and the exact endpoint-index
budget fixture was repinned accordingly.

Native MSVC CTest passed 49/49 in both build trees and Emscripten CTest passed
48/48. All nine filtered native/WASM parity validators passed; normalization is
2,360 bytes at SHA-256
`ea874451d9f86f3c30eae894562a68510f0b99e5fac194c856399d137d437252`.
Python passed 61/61 and the promotion manifest passed 15/15. Ruff, Pyright, uv
lock, clang-format, diff hygiene, and wn-dev-std repository/plan/test/language/
CI/compatibility audits passed. The governed native archives are identical at
45,738,054 bytes and SHA-256
`2a20b0398d568e45fc7175f8b67f228e77d8c47cc2f0ef627cd8bb6aae112626`.

The algebraic solver remains non-primary and production dispatch remains
disabled. Canonical packet/source-set assembly, diagnostics/digest, dispatch,
and transport integration remain pending.
