+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T133000-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T13:30:00-04:00"
+++

# Log: bounded exact rectangle enumeration

The exhaustive synthetic lane now covers all nine nonempty axis-aligned
rectangles on the integer `0..2` grid. It executes one-stage union/difference,
every ordered two-rectangle/two-operation combination, and every three-stage
operation sequence over a representative lower-left/full-grid/upper-right
subset: 558 deterministic exact cases.

An independent four-unit-cell oracle supplies expected material, area, and
four-neighbor component count. Every exact arrangement face must agree with an
independent ordered-stage replay. Final normalized output must contain lines
only, have exact shoelace area equal to occupied unit-cell area, and publish
the expected ring/region count. The complete per-case signature is
digest-locked across native and Emscripten.

This closes the first bounded exhaustive rectangle subset, not general
line/arc enumeration, seeded property generation/minimization, broader
normalization-collapse families, or production cross-transport execution.
Production solver dispatch remains disabled.

Validation passes: CTest 35/35; focused Emscripten build and 558-case
native/Emscripten digest parity; promotion-manifest tests 14/14; Ruff;
Pyright; clang-format; Lizard; generation and documentation freshness; uv
lock; and wn-dev-std 2026.8.12 repository, docs.plans, root-language, and
nested Rust audits. Full Rack reports 78 passed with only the intentional
active-plan L99 sentinel failing.
