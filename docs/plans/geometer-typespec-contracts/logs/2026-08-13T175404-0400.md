+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T175404-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T17:54:04-04:00"
+++

# Log: exact-backend monotonic budget remediation

Focused re-review found that rollback of all transaction state could refund
already executed algebraic work, permitting unlimited retries, and that the new
work-unit maximum was missing from the frozen packet's exhaustive limits table.

The design now separates rollback-only semantic/live-storage state from
monotonic executed-work counters. Phase reservations are charged before work
and remain consumed once a phase begins, even if that phase or a later phase
fails; unstarted reservations consume nothing. Nodes, values, caches, and live
storage still publish only on complete success. The feasibility vectors must
exercise a completed phase followed by a later reservation failure, semantic
rollback, persistent work consumption, and retry against the reduced budget.

The frozen packet now lists exactly 1,000,000,000 algebraic work units per job.
Python asserts the exact catalog value and exact documentation row. The packet
SHA-256 lock is updated to
`a8e52eb0fb13ae434e5bb1bfccbb3796998a70b4ed3c9e10c2d55d3fd2628dc8`.
Implementation and production solver permissions remain false.

Validation: focused promotion-manifest tests pass 11/11; full contract
freshness and 22 vectors pass; Ruff, Pyright, Biome, diff checks, and
wn-dev-std 2026.8.12 repository/docs.plans audits pass.
