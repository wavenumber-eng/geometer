+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T105500-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T10:55:00-04:00"
+++

# Log: typed result-graph resource and closure remediation

Independent review packet
`reviewer-01a000bc-5f04-701e-99b5-86ef40eb721c` identified two P1 defects in
the typed A0 result-record layer at commit
`386f850766250c5243208780c2b5512a654441a2`.

Ring job ownership formerly found every root with a fresh parent walk, making
a valid deeply nested hierarchy quadratic. The validator now memoizes every
ring root while unwinding each previously unseen chain, so every parent edge is
traversed at most once before the linear owner-assignment pass. A 16,384-level
alternating outer/hole hierarchy is the focused bounded-work regression.

Source tables formerly proved that every source reference belonged to an
interned set, but did not prove the reverse closure from logical result records
to every nonempty set. The validator now marks nonzero handles from vertices,
fragments, regions, and operand events and rejects any source-set record that
is not reached. A lexicographically valid appended source/set/index triple is
the focused canonical-byte mutation.

The governed 1,220-byte typed result vector remains unchanged with SHA-256
`37fe2c16c59d2f39d7900300883b80cc1412f343774bef81b6a9d8aea69df93f`,
and native/Emscripten bytes still agree. `production_solver_allowed` remains
false. Validation and the replacement review commit are recorded in the
corresponding executor review packet.
