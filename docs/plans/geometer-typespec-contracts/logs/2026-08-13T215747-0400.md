+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T215747-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T21:57:47-04:00"
+++

# Log: exact analytic intersections and certified nm normalization

The accepted exact scalar, expression, construction-DAG, and complete-artifact
substrate now supports the three authoritative analytic carrier intersections.
Line/line classifies unique, parallel-disjoint, and same-domain cases. The
line/circle and circle/circle solvers use exact discriminants and construction
nodes for rational or quadratic-irrational coordinates, distinguish disjoint,
tangent, secant, contained, concentric, and coincident cases, and return finite
points in exact lexicographic coordinate order. Invalid zero-length lines and
nonpositive radii reject without publishing construction nodes.

Composite construction is transactional: successful finite intersections
publish their normalized nodes, while disjoint/coincident/domain/resource
outcomes roll back semantic and owned-storage state but retain executed work.
Every mutating builder step is explicitly sequenced, so C++ function-argument
evaluation order cannot alter allocation order, work accounting, construction
keys, or native/Emscripten artifact bytes.

Final-grid coordinate normalization now implements nearest integer nanometer
with ties away from zero. Rational values use exact quotient/remainder tests;
irrational values refine a certified dyadic interval, use an exact integer
boundary predicate when the enclosure straddles one integer, and compare
exactly against the half-grid boundary. The whole public operation precharges
deterministic big-integer work and transient storage and contains allocation or
estimate exceptions. Exact success and one-unit-short preflight boundaries
are governed.

The fixed secant vector contains `(1,-sqrt(3))` and `(1,sqrt(3))`. Its complete
reachable `GEXPA001` artifact is 1,248 bytes with SHA-256
`0af1965a293c1b7a90ec633d35ee98f2d53098350e86e922e8c18751ea15a5e0`.
Native and Emscripten executables emit identical artifact bytes and the same
41,481,416-unit successful construction boundary; the parity validator checks
both values against the promotion manifest.

This remains exact-backend feasibility, not production Boolean integration.
The governed manifest keeps `production_solver_allowed = false`. Complete
curve-domain splitting, exact arrangement topology, face classification,
lineage, production packet dispatch, and the larger synthetic/mutation corpus
remain gated behind later slices.

Validation at the fixed pre-review tree: governed native validation and CTest
20/20; all ten exact Emscripten CTests; manifest-locked parity validator;
promotion-manifest tests 12/12; Ruff; Pyright; uv lock; clang-format; Lizard;
wn-dev-std 2026.8.12 repository, docs.plans, and language audits; and Rack 71
passed with only the intentional active-plan L99 hygiene sentinel failing.
