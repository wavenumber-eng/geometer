+++
type = "plan_log"
id = "generated-cpp-and-generic-abi-implementation"
plan_id = "geometer-typespec-contracts"
step_id = "pilot-contracts"
created = "2026-08-12T20:57:05-04:00"
+++

# Log: generated C++ and generic ABI implementation

The pilot now includes the authored `geometer.operation.outcome.a0` TypeSpec
root, generated success/failure schema and HTML reference, and governed success,
failure, and mixed-envelope vectors. The result union intentionally admits only
the implemented `geometry.model_bounds.a0` result.

The normalized catalog deterministically generates committed C++17 wire DTOs
and strict RapidJSON codecs. Decoders preserve optional-field presence and
reject malformed UTF-8 JSON, trailing data, duplicates, unknown fields,
literal/enum mismatches, non-finite numbers, and violated length/range
constraints. Generation and check mode both format with the repository
clang-format policy and fail on unsupported catalog constructs.

A focused typed operation registry maps generated model-bounds options into the
existing public C++ value API and maps its result back into the generated wire
DTO. The approved additive generic attachment C ABI is implemented with the
reviewed wasm32/pointer64 layout assertions, raw invocation limits, local ABI
failure classes, typed contract/operation outcomes, catalog discovery, owned
result handles, and borrowed accessors. All prior per-operation symbols remain.
The full browser target exports the generic ABI; the planar-only target remains
unchanged.

Native CMake/CTest validation passes all nine tests, including a real STEP
model-bounds round trip. The full WASM build succeeds, and Node smoke tests pass
for the new wasm32 generic ABI plus retained STEP-to-GLB and packed-planar
interfaces. The WASM tests now have their own Rack stratum. Contract generation
checks validate 19 vectors; focused Python/TypeSpec/docs tests report 43 passed,
and Ruff/Pyright pass.

The full Rack run remains non-green only because the active temporary
`docs/plans` tree is intentionally present; all other L99 checks pass.
Browser-level TypeScript, executable IPC, Rust, and public Python projections
remain later slices; no operation is promoted by this implementation.
