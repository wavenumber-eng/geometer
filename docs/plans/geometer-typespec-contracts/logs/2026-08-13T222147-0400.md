+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T222147-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T22:21:47-04:00"
+++

# Log: exact curve-domain and canonical split-order feasibility

The exact analytic feasibility backend now supplies the carrier and finite-
domain predicates needed before arrangement construction. Exact line carriers
compare projective direction and incidence. Exact circle carriers compare
center coordinates and positive radius. Closed line-segment and circular-arc
membership use only exact construction values and predicates.

Canonical line ordering uses the first nonzero Cartesian coordinate and is
independent of authored direction. Canonical circle ordering starts at the
negative-X seam and proceeds counterclockwise through the lower and upper
half-planes using exact signs and cross products; it does not use angles,
floating point, or hashes. Exact point equality provides the deduplication
predicate for the subsequent split-point grouping stage. Authored arc
membership validates circle incidence, nonzero radius, distinct endpoints,
direction, and coherent major-branch selection. Topology-indexed full circles
remain invalid and will use the governed two-half-arc result decomposition.

The construction transaction and builder were extracted into their own small
implementation module so these predicates share the already reviewed,
budgeted construction path without duplicating it. Every public predicate and
ordering entry point contains allocation exceptions and rolls back temporary
construction nodes on both semantic and resource failure.

The governed native/Emscripten vector covers carrier equality, segment
endpoints and exclusion, horizontal and vertical order, the circular seam,
both directions, minor and major branches, half turns, invalid full circles,
and a one-unit-short rollback boundary. It also replays irrational split
points constructed by an exact line/circle intersection through carrier
grouping, line/circle ordering, equality, segment membership, and arc
membership. Its canonical signature SHA-256 is
`56a531c6731e01d518f85bfdb046becc44d42a52dd81c0e1aab1f1aab917fcd8` and
the successful arc-membership work boundary is 710,842 units. Production
solver dispatch remains disabled; arrangement topology, face classification,
lineage, and the broader correctness corpus remain subsequent slices.

The previously enabled C++ foundation Rack stratum declared CTest but had no
discoverable Rack subtest, so Rack skipped it. A thin indexed wrapper now runs
the complete registered native CTest suite and makes that evidence visible in
the test index of record; it does not replace CTest or select a narrower test
set.

The wn-dev-std 2026.8.12 strict test-governance configuration now names the
repository test root and the existing `L99_release` signoff stratum. Two
disabled, empty legacy strata were removed from the Rack inventory: their
native pipeline and CLI coverage already lives in the now-executable C++
foundation and Python strata. No test implementation was removed.

Validation at the fixed tree: governed native validation and CTest 21/21;
all eleven exact Emscripten CTests; native/Emscripten curve-domain parity at
the frozen signature and 710,842-unit work boundary; unchanged exact-geometry
artifact parity; promotion-manifest tests 13/13; clean installed-wheel package
validation; Ruff; Pyright; uv lock; clang-format; Lizard; and wn-dev-std
2026.8.12 repository, plans, language, and strict test-governance audits pass.
Rack reports 73 passing tests with only the intentional active-plan L99
hygiene sentinel failing.

Independent review of `d38ee17bf478dd50c6653d636b434e3424325328`
accepted the exact curve-domain implementation but found that the new Rack
wrapper did not recognize the documented `cmake --preset default` build tree.
Discovery now prefers a CTest tree at `build/` configured for this repository,
then permits only the exact host-platform and host-architecture
`build-native-*` fallback configured for this repository. Regression fixtures
cover default precedence, coexistence with cross-platform and cross-
architecture trees, and rejection of a matching but stale foreign-root cache.
The focused discovery suite passes 4/4, strict test-governance audit passes,
and the full Rack replay now reports 76 passing tests with only the same
intentional active-plan L99 hygiene sentinel.
