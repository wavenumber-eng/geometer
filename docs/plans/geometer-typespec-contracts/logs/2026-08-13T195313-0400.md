+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T195313-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T19:53:13-04:00"
+++

# Log: deterministic factor and root-selection feasibility

Added deterministic integer factorization for primitive resultant candidates
using resource-bounded Kronecker evaluation, signed-divisor enumeration, exact
rational interpolation, and exact integer-polynomial division. Every possible
factor degree through half the source degree is exhausted before a polynomial
is classified as irreducible; a divisor-space or work ceiling returns
`resource_limit_exceeded`, never a speculative irreducibility answer.

Factors are primitive-normalized, recursively split, and sorted by their exact
coefficient vectors. Exact Sturm counts over caller-supplied open dyadic
intervals select a unique factor and real-root ordinal. Intervals containing
multiple candidate roots fail closed for further refinement. The governed work,
predicate, refinement, and logical-storage budgets remain monotonic and typed.

Native and Emscripten fixtures factor the accepted same-polynomial sum candidate
`x(x^2-8)`, preserve the irreducible `x^4-10x^2+1`, select both positive
`sqrt(8)` and rational zero, reject ambiguous intervals, and cover storage
preflight exhaustion.

This remains feasibility infrastructure. Operation-driven interval refinement,
rational collapse into canonical values, exact value equality/order/sign,
construction DAGs, codecs, and production solver integration remain gated.

Validation passes all 14 native CTests, all four exact Emscripten tests, the 12
promotion-manifest tests, Ruff, Pyright, uv lock, clang-format, complexity,
repository audit, and docs.plans audit. Rack reports 71 passing tests and only
the intentional release-hygiene sentinel caused by this active temporary plan.
