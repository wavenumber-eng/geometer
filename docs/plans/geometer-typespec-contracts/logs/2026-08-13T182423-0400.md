+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T182423-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T18:24:23-04:00"
+++

# Log: exact rational interning and Boost verification remediation

Closed the follow-up review gaps in the budgeted rational foundation while
leaving polynomial/root work and the production solver gate unopened.

Rational arena insertion is now a separately charged, monotonic phase. Its
checked deterministic bound covers reading retained bigint limbs, comparing the
candidate with every retained value, visiting arena slots, and possible slot
relocation during container growth. The arena maintains a retained-limb total
without rescanning values during preflight. Multi-entry tests prove increasing
lookup charges, canonical duplicate behavior, a failure after normalization but
before insertion, unchanged semantic/storage state, and reduced work available
to a retry.

Default CMake configuration now invokes both the pinned Boost restore/readiness
check and explicit official-archive SHA-256 verification. A custom
`GEOMETER_BOOST_ROOT` remains supported only when its `BOOST_VERSION` macro is
exactly 109200. Provenance tests reject both a missing archive and a present
archive with the wrong digest. The exact-backend resource-accounting design and
its manifest digest lock now state the insertion-phase rule explicitly.

Validation: native CTest passes 11/11. Rack passes 71 tests across Python,
TypeSpec, contract docs, WASM, TypeScript, Rust, and the non-sentinel L99 checks;
the only failure is the intentional active-plan hygiene sentinel.
