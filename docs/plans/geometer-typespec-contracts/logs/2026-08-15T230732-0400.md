+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-15T230732-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-15T23:07:32-04:00"
+++

# Log: filtered-analytic-solver-design

# Integrated overlay early-preflight remediation

Closed the performance-review blocker on `6a572b12b53cb3a0ccd9a748ac45cacefc64cc42`. The integrated filtered overlay now rejects a job whose unavoidable retained candidate results plus curve/carrier-group tables cannot fit before invoking the narrow phase. It also rejects when the minimum successful work (`curve_count + 2 * candidate_count`) exceeds the predicate budget. Both paths return with zero narrow predicate calls, zero narrow logical storage, and no output allocation. The canonical 256-byte pair charge is shared from the narrow-phase header so native, WASM, and downstream stages cannot drift.

The parity corpus now includes the impossible combined-memory and zero-work cases. Its filtered-overlay vector is 1,048 bytes at SHA-256 `b0f9e8013eeb02b876ce071b13663dbcae7dddf9f616432add5d1a72640c7438`. ADR-013, the production design, active plan, promotion manifest, and manifest tests record the allocation-free preflight.

Validation: native CTest 43/43 in both `build/` and the newly restored `build-native-windows-x64/` tree; WASM CTest 42/42; all 22 native/WASM parity validators; Python 61/61; manifest 15/15; Ruff, Pyright, uv lock, clang-format, touched-file Lizard, diff hygiene, and wn-dev-std repository/plan audits. L99 remains 8/10 only for the two pre-existing plan-wide conditions: request-packet validator CCN 113 exceeds 100, and the active temporary plan remains under `docs/plans`.

Production dispatch remains disabled. After independent re-review, the next large slice is global vertex reconciliation, half-edge arrangement construction, and nonquadratic face classification.
