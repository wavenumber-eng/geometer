+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-16T094500-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-16T09:45:00-04:00"
+++

# Log: filtered-analytic-solver-design

# Branch-safe filtered lineage publication

## Scope

Remediated independent review findings on the transition-driven filtered
lineage slice. The production path remains coordinate-free, speed-first, and
non-dispatched; algebraic exact evaluation remains an oracle rather than the
primary solver.

## Implementation

- Replaced the region-wide lineage cutoff with an edge-local epoch comparison.
  Newly live coverage is intersected with a generation-stamped sparse reporter,
  so sibling traversal order cannot omit contributors and the implementation
  does not clear or scan every operand for every region.
- Added a branched union/difference/refill regression in both reflection orders.
  The contributor set is invariant and includes a positive operand that stays
  covered while becoming lineage-live across a sibling branch.
- Accounted the reporter arrays with target-independent logical bytes and
  reserved their fixed work before arrangement. Region contributor publication
  remains output-sensitive; the output-heavy fixture publishes 256
  region/source incidences without a face-by-operand replay.
- The allocation-free count pass now preflights the exact second traversal,
  raw fill, and tuple-sort work before publication vectors reserve capacity.
  One unit short reports `resource_limit_exceeded` with zero publication
  capacity. Retained-memory reconstruction now includes the arrangement
  outgoing-half-edge table.
- Total lineage work and peak memory now propagate owned-region telemetry on
  early failures. Unsupported swept-path source roles fail before arrangement
  until filtered swept lowering owns their governed occurrence-key validation.

## Verification

- Native CTest: 47/47 passed.
- Emscripten CTest: 46/46 passed.
- Filtered parity validators passed for overlay, arrangement, selection,
  regions, and lineage. Lineage is 3,672 bytes at SHA-256
  `01595ed8a3bdeaf3cc63ab63f5f3be25b2a579a5a76a1b4e0f29b57c4078280e`.
- Promotion-manifest pytest: 15/15 passed.
- Ruff, clang-format, and diff hygiene passed.

## Remaining work

Operand outcomes, one-time 1 nm publication/normalization, post-normalization
topology validation, production dispatch, and transport projections remain
pending. Filtered swept-path lowering remains unsupported. The exact/algebraic
solver stays outside the primary production success path.
