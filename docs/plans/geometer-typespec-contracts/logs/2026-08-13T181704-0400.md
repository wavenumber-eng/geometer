+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T181704-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T18:17:04-04:00"
+++

# Log: exact rational budget and provenance remediation

Closed the independent review finding against the first exact-rational
foundation without opening the production solver gate.

Bigint preflight now derives 32-bit logical limb and magnitude-byte counts by
read-only backend inspection; it does not copy or negate an input before budget
reservation. Rational normalization reserves a conservative logical bound for
the retained value, container slot, and every simultaneously live bigint
temporary. Allocation and checked-estimate failures at the public interning
boundary return `resource_limit_exceeded` with semantic and storage rollback
while completed work remains consumed.

Canonical integer and rational encoders now require a budget and return typed,
move-only results whose complete payload allocation remains charged for the
result lifetime. They perform checked size/work/storage preflight, build the
rational payload directly without uncharged intermediate byte vectors, and
convert allocation or estimate failures to the governed resource diagnostic.
Low-storage and low-work regressions verify fail-closed behavior.

Default CMake configuration now always passes through the pinned Boost restore
helper. Readiness requires the exact provenance sentinel and Boost 1.92 version
macros; explicit verification additionally requires the cached official archive
SHA-256. Python regressions cover version/sentinel tampering and a missing
archive. The exact-backend design now distinguishes deterministic logical owned
storage from the independent process working-memory ceiling, and its manifest
digest lock was refreshed.

Validation: native CTest passes 11/11; Rack's Python, TypeSpec, contract-doc,
WASM, TypeScript, and Rust strata pass 71 tests. L99 passes 9/10 and reports only
the intentional active-plan sentinel after splitting the touched manifest test
below the function-size limit. Ruff, Pyright, clang-format, uv lock, and the
focused provenance/promotion tests pass.
