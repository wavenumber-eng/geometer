+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T080039-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T08:00:39-04:00"
+++

# Log: exact result-region merging and source associations

Exact selected faces now merge into maximal interior-connected result regions.
Faces join only across a shared analytic edge whose two sides are material;
point contact never joins area components. Boundary traversal crosses internal
material seams without serializing them and retains only half-edges separating
material from empty space.

The region component graph is rooted at the unbounded empty face. It derives
acyclic ring parent/depth links, outer/hole winding parity, and distinct nested
islands without geometric sampling. Rings rotate and sort by canonical
vertex/analytic half-edge keys. Result regions sort by their outer-ring keys.
Each region carries the unique sorted union of positive lineage from all of its
cells, and the projection emits canonically sorted many-to-many source/result
associations.

The shared-edge rectangle fixture exposed an over-restrictive arrangement
precondition: a valid planar DCEL vertex may have odd valence after coincident
edges collapse. The arrangement now accepts degree-three shared-edge endpoints;
the existing minimum-degree, twin, next/previous, face, and coverage invariants
remain enforced.

Closed-form vectors prove one region with two contributors and no shared seam,
two point-tangent regions sharing one vertex and one contributor, same-stage
operand permutation invariance, and an outer/hole/island parent chain with
separate contributor lineage. One-unit-short work and one-byte-short storage
limits fail closed without publishing a result or leaking logical storage.

The native/Emscripten canonical signature SHA-256 is
`b33cc58943c8346a8bc54232fbe7cc4a55ca35d10f9e4038be9be3e88cbacc2f`.
The nested region projection boundary is 904 work units and 5,760
conservatively reserved logical bytes.

This remains exact feasibility infrastructure, not the production operation.
Boundary/vertex provenance projection, operand outcomes, final certified-nm
normalization and packet encoding, OCCT audit, broader synthetic/property/
mutation coverage, and production dispatch remain pending;
`production_solver_allowed` remains false.

Validation passes: native validation and CTest 24/24; governed exact
Emscripten build and native/WASM region parity; promotion-manifest tests 13/13;
Ruff; Pyright; uv lock; clang-format; Lizard; and wn-dev-std 2026.8.12
repository, docs.plans, and language audits. Full Rack reports 77 passed with
only the intentional active-plan L99 sentinel failing.
