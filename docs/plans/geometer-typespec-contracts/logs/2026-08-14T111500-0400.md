+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T111500-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T11:15:00-04:00"
+++

# Log: semantic canonical result projection

Before semantic result goldens were frozen, implementation exposed one omitted
batch tie in the A0 packet prose: two caller jobs may produce identical
geometry, while job-owned topology cannot be shared across standalone job
closures and the published local keys contained no allocator-independent
tiebreaker. Independent clarification packet
`reviewer-01a000c8-8fb8-73b2-93b0-505315c71f09` accepted caller job id as the
leading outer key for job-owned vertices, fragments, rings, and result regions,
followed by every existing complete semantic key. Global immutable source
content remains interned by value. The packet generation and magic are
unchanged; the clarified packet-spec digest is
`83e97d3636a93a8358777f459e4dbee70cfd88c6d16b28b683c2f8a538eb7727`.

The new C++17 projector validates its input graph and deterministically assigns
canonical vertex keys from coordinates, sorted analytic incidence signatures,
and intersection sources; fragment keys from canonical endpoints, complete
analytic replay scalars, and provenance; linear-time least ring rotations;
recursive parent-key ranks; owner-prefixed ring and result-region order; and
rebased generated ids and references. Operand-event references and records are
re-sorted after topology rebasing, and relationship region ids/pairs are
rebased and sorted. Source references and source sets retain the separately
implemented global content interning authority.

Permutation fixtures prove that different vertex/fragment allocation orders
and ring start rotations converge byte exactly and that projection is
idempotent. A mixed-batch fixture gives two jobs identical geometry in opposite
physical table orders; both converge while remaining distinct under caller job
ownership. The governed canonical vector is 1,220 bytes with SHA-256
`07019a4c2f94f8066db90b6f57bb3e5ea7801bb6b8bda63d8c7268dcad5bcb72`,
and native/Emscripten bytes agree.

This slice does not yet extract standalone job closure, compute SHA-256 job
digests, require canonical bytes at the raw decoder boundary, or enable
production dispatch. Those remain subsequent slices and
`production_solver_allowed` remains false.

Validation passes: native CTest 30/30; native/Emscripten strict-record and
semantic-canonical byte parity; promotion-manifest tests 13/13; Ruff; Pyright;
uv lock; clang-format; Lizard; generated contract/documentation freshness;
browser-WASM, TypeScript, and Rust strata; and wn-dev-std 2026.8.12 repository,
docs.plans, root-language, and nested-Rust language audits. Full Rack reports
77 passed with only the intentional active-plan L99 sentinel failing.
