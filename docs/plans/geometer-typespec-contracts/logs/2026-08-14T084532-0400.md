+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T084532-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T08:45:32-04:00"
+++

# Log: exact governed operand outcomes

The exact arrangement pipeline now derives all seven catalog-governed operand
outcome events from positive-area face histories. Union operands report final
contribution, same-stage or preexisting absorption, partial later removal,
complete later removal, or exclusive no-effect as their exact histories
require. Difference operands receive independent same-stage removal credit and
report surviving boundary effects, later refill, or exclusive no-effect.

Events are canonical by operand id and event code. Final contribution names
exactly the associated result regions. Surviving subtraction names the exact
boundary rings and their material-side result regions. Every event shares the
operand's complete sorted authored/compact source set; packed source-set
interning remains part of the later result-packet projection.

The builder recomputes ordered stage semantics for every face, validates
complete region contributor sets against exact material components, proves
complete material-boundary/ring closure, validates occurrence/source catalog
tuples and carrier kinds, and checks subtraction provenance against empty-side
history. Mismatched stages, regions, provenance, or source metadata fail closed.
Work, storage, event, reference, stage, operand, and occurrence limits are
preflighted before an outcome object is exposed.

Three closed-form stage programs cover all seven events, including coincident
same-stage positives, partial removal, a surviving subtraction partly restored
by a later union, a completely removed positive, and a difference wholly
inside an already empty area. Operand permutation within an unordered stage is
byte-signature invariant. Malformed source-role and relabeled-stage regressions
fail closed, as do one-unit-short work and one-byte-short storage boundaries.

The native/Emscripten canonical combined signature SHA-256 is
`e8cfe16cf62b1f074537415eb9a8ddfe2d9d7d6bdebb70abe152d8b3d82744af`.
The primary fixture boundary is 1,600 work units and 6,016 conservatively
reserved logical bytes.

This remains exact feasibility infrastructure. Final certified-nm result
normalization and packet encoding, the OCCT audit, broader invariant,
metamorphic, degeneracy, exhaustive, property, cross-runtime, and mutation
corpora, and production dispatch remain pending; `production_solver_allowed`
remains false.

Validation passes: native validation and CTest 26/26; governed exact
Emscripten build and native/WASM outcome parity; promotion-manifest tests
13/13; Ruff; Pyright; uv lock; clang-format; Lizard; and wn-dev-std 2026.8.12
repository, docs.plans, and language audits. Full Rack reports 77 passed with
only the intentional active-plan L99 sentinel failing.
