+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T194050-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T19:40:50-04:00"
+++

# Log: square-free resultant candidate feasibility

Added the next exact-backend feasibility layer after independent acceptance of
the root-budget and Thom-zero remediation. A fraction-free Bareiss determinant
over integer-polynomial Sylvester matrices now constructs exact sum and product
resultant candidates. Reciprocal and nonnegative-square-root candidate
polynomial transforms use the same primitive and square-free normalization
path.

Every transform and resultant phase performs checked degree, coefficient-growth,
work, and logical-storage preflight before allocation or arithmetic. Candidate
degree remains capped at 64 and coefficient estimates at 16,384 bits. Temporary
storage rolls back on typed failure while started work remains consumed.
Unknown operations and impossible candidate bounds fail before work.

Native and Emscripten fixtures cover monic and nonmonic sums/products,
square-free repeated-result removal, reciprocal and square-root transforms,
composition with Sturm root isolation, structural rejection, and resource
exhaustion. In particular, `sqrt(2)+sqrt(3)` produces
`x^4-10x^2+1`, while the same-polynomial sum deliberately retains the
square-free multi-factor candidate `x(x^2-8)`.

This slice does not claim canonical algebraic value construction. Deterministic
integer factorization, exact minimal-factor/root selection, rational collapse,
value equality/order/sign, DAGs, codecs, and production solver integration
remain gated.

Validation passes all 13 native CTests, all three exact Emscripten tests, the
12 promotion-manifest tests, Ruff, Pyright, uv lock, clang-format, complexity,
repository audit, and docs.plans audit. Rack reports 71 passing tests and only
the intentional release-hygiene sentinel caused by this active temporary plan.
