+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T095349-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T09:53:49-04:00"
+++

# Log: canonical provenance source-set tables

The exact analytic backend now projects logical provenance sets into the three
canonical result-packet tables: globally sorted unique source references,
lexicographically sorted and content-interned nonempty source sets, and the
gapless source-reference-index membership table. Empty logical sets map only to
handle zero; nonempty handles are deterministic one-based ordinals. Input
ordering, duplicate members, and duplicate sets cannot affect the tables.

The builder validates the exhaustive A0 source-kind, role, id, and swept-
boundary-occurrence-key combinations before allocating output. Authored curve,
compact primitive/capsule, swept offset/join/cap, and subtractive-effect roles
are all covered. Invalid cross-kind roles and malformed occurrence keys fail
closed.

Portable peak storage and deterministic work are charged before allocation.
The governed overlapping-set vector proves `{A,B}`/`{A,C}` indirection without
duplicating `A`, duplicate-set interning, empty handle zero, and stable input
handle rebasing. One-unit-short work and one-byte-short storage boundaries fail
without retained storage. Native and Emscripten agree on SHA-256
`dc19f5249a91d159f0ed1dff4ff78617315c157050f1df09fe47d834986daffc`,
5,760 work units, and 6,272 charged bytes.

Result-packet record encoding/decoding, standalone job closure/rebasing and
digest computation remain pending follow-on work. Production dispatch remains
disabled and `production_solver_allowed` remains false.

Validation passes: native CTest 28/28; native/Emscripten governed source-set
parity; promotion-manifest tests 13/13; Ruff; Pyright; uv lock; clang-format;
Lizard; diff checks; and wn-dev-std 2026.8.12 repository, docs.plans, and
language audits. Full Rack reports 77 passed with only the intentional active-
plan L99 sentinel failing.
