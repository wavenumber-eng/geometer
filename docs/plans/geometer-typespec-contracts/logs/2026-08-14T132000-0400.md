+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T132000-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T13:20:00-04:00"
+++

# Log: exact Boolean identity properties

The synthetic-correctness lane now evaluates four regularized Boolean identity
properties through the exact arrangement, selection, region, provenance,
outcome, and final normalization pipeline.

`union(A, A)` uses coincident rectangles with distinct coverage and operand
identities. Its normalized analytic geometry must equal the baseline while
both contributors survive in region lineage and receive symmetric contributor
and absorbed-coverage events. `difference(A, A)` must succeed with an empty
normalized result, completely remove the positive operand, and never classify
the effectful subtractor as `no_effect`. Zero-operand union and difference
stages must leave geometry, selection lineage, and outcome events byte-equivalent
to the baseline.

Independent review found that the first revision only proved the subtractor
was not mislabeled `no_effect`; the implementation actually emitted no event
for complete subtraction because no final boundary remained. The governed A0
semantics now require `subtraction_effect_survives` for unfilled attributed
removal, with an empty reference range specifically when complete subtraction
leaves no final material boundary. The implementation emits that concrete
event, and the identity signature is derived from the actual outcome records.

Re-review then identified that an `overwritten` boolean records any refill,
not complete refill. Outcome simulation now tracks the active removal owners
per positive-area arrangement face, clears them on refill, and records which
owners still have unfilled final cells. A disconnected regression removes two
components with one operand, refills only one, and requires both
`subtraction_effect_survives` with zero references and
`subtraction_effect_overwritten_later`. This coexistence vector has its own
native/WASM digest lock.

The compact property signature is digest-locked and must match natively and
under Emscripten. This does not complete stage-splitting, mixed-batch identity,
exhaustive/property, broader lineage, or production cross-transport lanes.
Production solver dispatch remains disabled.

Validation passes: CTest 34/34; focused Emscripten build and native/Emscripten
digest parity; promotion-manifest tests 14/14; Ruff; Pyright; clang-format;
Lizard; generation and documentation freshness; uv lock; and wn-dev-std
2026.8.12 repository, docs.plans, root-language, and nested Rust audits. Full
Rack reports 78 passed with only the intentional active-plan L99 sentinel
failing.
