+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-16T053936-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-16T05:39:36-04:00"
+++

# Log: filtered-analytic-solver-design

# Face-selection performance re-review remediation

Closed the performance re-review findings on the indexed filtered
face-selection slice.

Arrangement admission now derives guaranteed noncollapsed spans and separated
collapsed vertices in one validated allocation-free scan. Curves, bounds, and
occurrences must have matching lengths, point enclosures must be valid, and the
single proportional traversal is bulk-charged before it begins. The public
minimum estimator therefore returns false for malformed parallel arrays rather
than indexing outside them.

Overlay precharges both support-distance checks for each adjacent cluster event
and all four checks for circular first/last fusion. Selection admission models
topology and persistent coverage as separate live phases, counts face outputs
once, and uses the guaranteed transition prefix to reserve canonical persistent
nodes and the fixed-capacity coverage table before arrangement work. The
already-charged face count is reused during publication, and stage-count
multiplication is widened and checked.

Focused native and WASM filtered tests pass 5/5. Native/WASM parity is exact:
overlay 1,048 bytes / `8c1162b7e14b6a58a2dd8f50c4c4b87767a223632c6393a3a8ace3d0244ac352`,
arrangement 13,032 bytes /
`c548d63bf8577eb615de5569dedef2d5cbff9ab7dccf831d24bb82ad08788d1d`,
and Boolean selection 12,360 bytes /
`56274596c6005d1def4c7197719836d4ea8730b3876472b336ba6a7571c5edb8`.
Promotion-manifest tests pass 15/15. Tests include mismatched estimator arrays,
malformed points, exact/one-short circular seam work, a non-power-of-two
persistent-coverage admission boundary, and early separated-collapsed rejection
with zero arrangement telemetry.

The complete native suites pass 45/45 in both build trees and the complete WASM
suite passes 44/44. Python passes 61/61; Ruff, Pyright, uv lock, package
validation, clang-format, focused Lizard thresholds, diff hygiene, and the
wn-dev-std repository/plan/test/language/CI/compatibility audits are clean.

Selected-boundary publication, complete lineage projection, final 1 nm
normalization, owned production dispatch, and transport integration remain
pending. The algebraic backend remains a non-primary oracle/fallback only.
