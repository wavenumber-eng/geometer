+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T170000-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T17:00:00-04:00"
+++

# Log: scheduled extended seeded-property profile

The exact rectangle property harness now has a separate `nightly` profile with
eight frozen SplitMix64 seeds and 16 cases per seed. Its 128 cases execute both
the baseline and same-stage operand reversal through the exact pipeline and
retain the same reducer and replay behavior as the pull-request profile.

Native and Emscripten agree on governed SHA-256
`0cc922aca1bd2718b330cf1d0d080882181aed861fb5edf6601ee5c5d96f56a5`.
The existing WASM workflow schedules the extended native/WASM comparison
monthly, reflecting Geometer's change rate, and exposes an explicit manual
input for release and kernel-change validation. The default pull-request
profile and its digest remain unchanged. Production solver dispatch remains
disabled.
