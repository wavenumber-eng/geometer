+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T155800-0400"
plan_id = "geometer-typespec-contracts"
step_id = "model-bounds-promotion"
created = "2026-08-13T15:58:00-04:00"
+++

# Log: model-bounds cross-language evidence closure started

Closed the final known shared-vector coverage gap before promotion. The C++17
projection now reads the governed manifest, decodes raw JSON and hexadecimal
invalid-UTF-8 inputs through each generated root codec, verifies all 20 declared
accept/reject results, and checks the three presence-projection vectors. This
matches the complete manifest replay already performed by TypeScript, Rust, and
Python instead of relying on a smaller set of handwritten C++ examples.

The focused native build and `geometer_operation_contract_test` pass locally.
The complete candidate revision
`03cefcfefbd6cd848eebac4f94d02929d8176d3c` then passed hosted workflow run
`31738634104`: Windows x64 job `94576270477`, macOS ARM64 job `94576270535`,
Linux x64 job `94576270581`, and Linux ARM64 job `94576270647` all succeeded.
Standards job `94576270400` failed only because the active temporary plan is
present; its other nine release checks passed. The promotion manifest records
these immutable identifiers plus SHA-256 locks for the catalog, vector
manifest, and browser JavaScript/WASM artifacts.

Promotion remains pending only for independent review of this evidence and the
complete vertical. No contract authority or operation status is changed by
this candidate evidence commit.

Independent review requested changes because the initial 20-vector corpus did
not yet fulfill the separately promised operation-semantic, diagnostic, and
toleranced transport-equivalence lanes. The remediation adds two governed
vectors: a real SOT-23 identity result with timing projection plus explicit
absolute/relative tolerance, and a non-affine transform with exact diagnostic
fields. C++ replays both through the generic C ABI, TypeScript through browser
WASM, Rust through the persistent executable IPC client, and Python replays the
successful expected projection through its compatible generated boundary. The
native, WASM, and IPC lanes all use the same expected projection, making their
equivalence assertion independent of timing and JSON spelling. Promotion stays
pending until this remediation receives fresh local, hosted, and independent
evidence.

The first hosted remediation run exposed a useful oracle defect: the expected
source hash had been frozen from a Windows CRLF checkout of the text STEP
fixture, so Linux and macOS correctly produced a different hash for their raw
LF attachment bytes. Source hash is now a manifest-declared computed field,
not an exclusion. C++, TypeScript, Rust, and Python independently compute exact
FNV-1a from the attachment they submit and compare it to the returned source
hash; the expected geometry projection retains only its structural placeholder.
This preserves exact source identity while preventing checkout policy from
being mistaken for geometry divergence.

The rejecting evidence is workflow run `31740395395` at
`ebf08048449740e43d8489f05e2fa1365b8f50d1`: Windows x64 passed, while macOS
ARM64, Linux x64, and Linux ARM64 all rejected the Windows-literal source hash
in the native C++ vector. This run is retained as evidence that the corrected
computed-field oracle detects the exact platform-dependent condition it was
introduced to govern; it is not promotion evidence.

Corrected exact candidate
`e035bcd348cf0d8aa3db1812d907c19a36690ea0` passed hosted workflow run
`31741067434`: Windows x64 job `94584293053`, macOS ARM64 job `94584293148`,
Linux x64 job `94584293062`, and Linux ARM64 job `94584293143` all succeeded.
Standards job `94584292990` failed only on the intentional active-plan hygiene
sentinel. The remediated promotion packet is therefore ready for independent
re-review; no authority transition is claimed before acceptance.
