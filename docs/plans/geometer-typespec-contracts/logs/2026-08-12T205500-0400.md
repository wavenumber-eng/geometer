+++
type = "plan_log"
id = "pilot-contract-vectors-started"
plan_id = "geometer-typespec-contracts"
step_id = "pilot-contracts"
created = "2026-08-12T20:55:00-04:00"
+++

# Log: pilot contract vectors started

The model-bounds pilot now has a language-neutral raw-fixture manifest with
explicit strict-JSON, generated-schema, and presence-projection lanes. The
initial corpus protects duplicate-key and trailing-data rejection, closed
canonical objects, aliases that belong only at compatibility boundaries,
nullable and matrix-shape rejection, the diagnostic root, the current result
shape, timing exclusions, and absent-versus-explicit-default patch presence.

The dedicated TypeSpec Rack check replays these vectors after deterministic
generation. The maintained model-bounds compatibility report records current
handwritten acceptance of empty/null roots, aliases, uppercase STEP, nested
matrices, and unknown fields without treating those behaviors as canonical
TypeSpec structure.

The pilot step remains active. Cross-language strict codecs, diagnostic paths,
semantic operation vectors, numeric tolerances, and live transport replay are
still required before promotion.
