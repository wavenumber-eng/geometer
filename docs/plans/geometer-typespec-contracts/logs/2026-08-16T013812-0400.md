+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-16T013812-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-16T01:38:12-04:00"
+++

# Log: filtered-analytic-solver-design

# Arrangement admission-work remediation

Closed the remaining exact-commit performance-review blocker in the filtered
arrangement admission pass. The allocation-free carrier-token scan now
bulk-charges one shared work unit per input curve before traversal. A ceiling
too small for the complete scan returns without scanning or invoking the owned
narrow/overlay stage. Successful and downstream-rejected jobs retain the scan
charge in explicit `admission_work_units` and total predicate telemetry, while
overlay telemetry remains zero on early rejection.

The owned overlay receives only the remaining predicate allowance, and the
arrangement builder starts from admission plus overlay work. This prevents the
new preflight traversal from increasing the effective job ceiling. Exact and
one-short arrangement budgets remain inclusive and deterministic. The
32-disjoint-square downstream fixture now distinguishes admission work from
upstream overlay work, and a one-unit-short admission fixture proves the bulk
charge occurs before traversal.

Native CTest passes 44/44 in both build trees and WASM CTest passes 43/43. The
canonical native/WASM vector is byte-identical at 11,392 bytes with SHA-256
`ba79e8b87448542a5a4c1a5f1030f9beca295997ec7cd748707772db664dfb48`.
The promotion-manifest tests pass 15/15; Ruff, Pyright, uv lock, clang-format,
diff hygiene, and the wn-dev-std repository/test/language/CI/compat audits are
clean. The design, ADR, plan, and manifest now state that the proportional
admission traversal is governed rather than free.

Next after independent performance re-review: indexed face ownership and
ordered Boolean material classification.
