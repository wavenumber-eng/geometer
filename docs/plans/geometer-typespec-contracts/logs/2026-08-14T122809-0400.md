+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T122809-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T12:28:09-04:00"
+++

# Log: between-stage normalization mutation sentinel

The exact result-normalization lane now protects the final-stage-only grid rule
with an ordered Boolean mutation. The authoritative case unions a rectangle
ending at `7/5` nm, then subtracts its exact `[6/5, 7/5]` tail. The mutation
prematurely snaps the first-stage rectangle to 1 nm before applying the same
subtraction. Both paths deliberately normalize to the same final integer-grid
rectangle, so a geometry-only comparison cannot detect the defect. The exact
selection lineage does: the authoritative path retains source 200's
subtraction effect, while the prematurely normalized path incorrectly reports
no effect.

The fixture uses an explicitly atomic overlapping-tail arrangement so the
comparison does not rely on OCCT or an unimplemented curve-splitting stage.
Native and Emscripten report the same governed
`between_stage_normalization` mutation inventory, while the existing normalized
geometry vector and digest remain unchanged. Production analytic dispatch
remains disabled.

Validation passes: Windows native validation and CTest 32/32; focused
Emscripten build and native/Emscripten normalization-vector and mutation
parity; promotion-manifest tests 14/14; Ruff; Pyright; clang-format; Lizard;
generation/package freshness; uv lock; and wn-dev-std 2026.8.12 repository,
docs.plans, root-language, and nested Rust audits. Full Rack reports 78 passed
with only the intentional active-plan L99 sentinel failing.
