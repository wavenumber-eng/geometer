+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-16T042636-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-16T04:26:36-04:00"
+++

# Log: filtered-analytic-solver-design

# Indexed filtered face ownership and ordered material selection

Implemented the next production filtered-solver stage behind an internal
boundary that accepts ordered request records, trusted lowered geometry, and
canonical broad-phase pairs and owns arrangement construction. A certified
vertical-slab sweep processes complete x-event columns, maintains crossing
branches in a fixed-capacity implicit AVL sequence, and joins cycle sides that
border the same open gap with a governed disjoint set. This assigns nested and
disconnected boundary components without cycle-pair containment scans and
performs no second 50 nm repair.

Added lowering-issued vertical construction-column identities, exact
axis-aligned line-intersection fast paths, preserved correlated point-column
facts through the existing resolution merge, and canonical circle seam
handling. These close odd-width vertical capsule and seam-adjacent arc cases
without algebraic fallback. Unrelated overlapping coordinate intervals remain
fail-closed.

Sorted edge memberships now become sparse face-dual coverage transitions.
Canonical persistent operand-state roots avoid copying every operand onto every
face, while an indexed stage tree applies ordered union/difference semantics
only where coverage changes. Tests cover nested rectangles and circles,
siblings, annuli, irrational overlapping and tangent disks, odd-width capsules,
all sixteen four-stage rectangle add/difference combinations against an
independent area oracle, sparse scaling, and exact/one-short work and memory
budgets.

The implementation was split at the repository hygiene gate into a 1,372-line
face/coverage module, a 227-line integrated admission module, and a 657-line
private indexed-state support header. Touched-source Lizard reports no CCN,
length, or parameter warnings under the release thresholds.

Validation: native CTest passes 45/45 in both `build/` and
`build-native-windows-x64/`; WASM CTest passes 44/44. All 24 native/WASM parity
validators pass. The filtered selection vector is byte-identical at 10,856
bytes with SHA-256
`08a9b9cf7234cef5420461ec2e81bc79afe63e963716d80033192f9daf42a8e3`.
Python passes 61/61 and the promotion manifest passes 15/15. TypeSpec,
contract-doc, WASM, TypeScript, and Rust Rack strata pass; native runtime,
examples, and clean-wheel package validation pass. Ruff, Pyright, uv lock,
clang-format, diff hygiene, and wn-dev-std repo/test/language/CI/compat audits
are clean.

Rack reports 88 passing and the same two plan-wide release conditions: the
pre-existing request-packet validator has CCN 113 against the release threshold
of 100, and the active temporary plan remains under `docs/plans`. The new
selection module no longer contributes a hygiene failure.

Production dispatch and algebraic fallback remain disabled. Next after
exact-commit numeric, performance, and integration review: selected-boundary
publication, complete lineage projection, and final 1 nm normalization within
the fixed 50 nm topology envelope.
