+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T213136-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T21:31:36-04:00"
+++

# Log: normalized construction DAG and complete exact artifact

The exact feasibility backend now implements the closed A0 construction-node
catalog: rational, associative sum, associative product, reciprocal, and
nonnegative square root. Sum and product flatten same-kind children, sort
rational and nonrational inputs before evaluation, fold rational terms, remove
identities, short-circuit zero products, and sort final children by unsigned
`(value key, construction key)` bytes. Exact evaluation that produces a
rational collapses to the unique rational node. Linear interning charges every
candidate/existing key comparison and prospective arena relocation before
lookup or growth; failed transactions retain executed work while rolling back
new nodes and storage.

The complete `GEXPA001` artifact encoder emits only nodes reachable from the
ordered semantic roots. It sorts nodes by dependency depth and then complete
construction key, remaps children to backward-only canonical indices, preserves
root order, and writes the frozen 32-byte header plus minimally aligned node
records and root table. Planning charges graph edges and worst-case key
comparisons before traversal or sorting.

The strict decoder validates magic/generation/flags/reserved fields, exact total
and record lengths, minimum alignment, kind arity, backward child topology,
scalar canonicality, zero padding, canonical node order, duplicate interning,
evaluated value keys, normalized children, root bounds, and full reachability.
It rebuilds every node through the same exact construction APIs rather than
trusting serialized values. Returned arenas and root lists own governed storage.

Native and Emscripten tests cover commutation/reassociation, rational folding,
identities, rational collapse, equal values through distinct constructions,
allocation-order-independent keys and artifacts, unreachable-node elision,
semantic root order, a 92-byte rational-zero golden, strict structural
mutations, and exact success/one-unit-short encode, decode, and transaction
boundaries. Geometric intersection vectors and production solver integration
remain disabled for their next independent gate.

Validation at the fixed pre-review tree: native CTest 19/19; all nine exact
Emscripten executables; promotion-manifest tests 12/12; Ruff; Pyright; uv lock;
clang-format; Lizard; wn-dev-std repository, plan, and language audits; and Rack
71 passed with only the intentional active-plan L99 hygiene sentinel failing.
