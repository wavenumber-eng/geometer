+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T123000-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T12:30:00-04:00"
+++

# Log: first closed-form analytic invariant corpus

The synthetic-correctness program now includes its first OCCT-independent
closed-form oracle. Controlled normalized result packets cover a rectangle,
disk, concentric annulus, line-and-semicircle capsule, and nested rectangular
outer/hole/island topology. Every case must first pass exact topology replay.
An independent Green's-theorem implementation then computes exact
rational-plus-pi area and perimeter coefficients and checks component, hole,
and Euler-characteristic expectations.

Native and Emscripten emit the same governed signature. The promotion manifest
locks the five-case inventory, parity validator, and signature SHA-256.

The annulus fixture exposed avoidable exact-construction cost. Packet replay now
canonicalizes the proven zero center offset of a diameter-defined semicircle to
rational zero. Arrangement face nesting recognizes full circular cycles and
uses exact squared-distance containment against their common carrier instead of
an unnecessary algebraic ray cast. The mixed line/arc ray oracle remains the
fallback.

This is a bounded first portion of the active synthetic-correctness step.
General-arc certified area/perimeter, tangent and coincident cases,
near-degenerate sweeps, metamorphic and exhaustive/property lanes, broader
lineage cases, and production cross-transport/client execution remain pending.
Production analytic dispatch remains disabled.

Validation passes: Windows native validation and CTest 32/32; focused
Emscripten build and native/Emscripten closed-form signature parity; promotion
manifest tests 14/14; Ruff; Pyright; clang-format; Lizard; generation/package
freshness; uv lock; and wn-dev-std 2026.8.12 repository, docs.plans,
root-language, and nested Rust audits. Full Rack reports 78 passed with only
the intentional active-plan L99 sentinel failing.
