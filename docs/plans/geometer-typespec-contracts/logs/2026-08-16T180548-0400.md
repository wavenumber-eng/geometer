+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-16T180548-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-16T18:05:48-04:00"
+++

# Log: filtered-analytic-solver-design

# Harden filtered packet accounting and source binding

Closed the packet-assembly review findings without changing coordinates,
topology, or the 50 nm policy. Authored provenance now binds its line/arc role
to the originating request segment kind, so cross-role forged trusted inputs
fail as invalid. All binary-search work in request/source/event binding is
precharged against the programmable work ceiling.

The fixed-capacity sequence interner now uses explicit target-independent live
phase formulas and size assertions for nodes, edges, traversal frames, output
ranges, handles, and serialized memberships. Packet assembly carries the
accepted normalization peak across the ownership transfer, accounts persistent
binding/source-use/source-table/publication capacities once, and checks each
source, incident, ring, event, and encoder scratch phase before allocation.
This removes allocator-capacity and native/WASM width assumptions from the
reported peak.

Added both authored line-to-arc and arc-to-line forgery regressions, a direct
exact/one-byte-short sequence-memory boundary, and 32/64 disjoint-disk packet
scaling. The doubled sparse workload remains below 3x work and memory, and its
actual publication work/memory stays within the candidate reservation.

Native MSVC CTest passed 50/50 in both build trees and Emscripten CTest passed
49/49. All ten filtered native/WASM parity validators passed; the packet vector
remains 4,264 bytes and is now SHA-256
`367c9bfeee1a993be77fc454bb7199b7500985d0d5a5db3eca16da0227a444fe`.
Promotion-manifest pytest passed 15/15 and Ruff/diff hygiene passed. The governed
native archives are identical at 47,762,306 bytes and SHA-256
`59832dc94574415b3866e989c64dfe81d31120aa3f177a27c65f61a4ee190254`.

The algebraic solver remains a non-primary oracle and production dispatch
remains disabled. Batch merge, relationship projection, operation dispatch,
transport integration, and the final release gates remain pending.
