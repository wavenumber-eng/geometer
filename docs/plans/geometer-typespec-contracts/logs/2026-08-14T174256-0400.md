+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T174256-0400"
plan_id = "geometer-typespec-contracts"
step_id = "occt-8-0-1-qualification"
created = "2026-08-14T17:42:56-04:00"
+++

# Log: occt-8-0-1-qualification

# Log: extended OCCT consumer qualification harness

The side-by-side V8_0_0 and V8_0_1 harness now covers the retained consumer
surfaces in isolated tag-specific dependency, build, distribution, and output
roots. In addition to native/WASM CTest and the exact/synthetic parity corpus,
the extended lane validates the native CLI and Python wrapper, STEP-to-HLR,
GLB, SVG, and planar outputs, the browser WASM compatibility surface, the
generated TypeScript package and Worker clients, and a real headless
Chrome/Chromium HLR execution.

Repeated WASM STEP-to-GLB calls retain output SHA-256 and heap-size evidence.
The comparison helper requires byte-identical GLB, SVG, request, repeated WASM
output, and full-browser results. It permits only the measured HLR timing
object and the generated STEP header timestamp to differ, with unit tests that
reject semantic differences outside those exclusions.

Both tags passed the extended lane during development. At this revision, Rack
reports 83 passed and only the intentional active-plan L99 hygiene sentinel.
The exact clean-revision tag reports and comparison will be regenerated after
this log and harness are committed, then submitted for independent review.
The production OCCT pin remains V8_0_0, caches remain unpublished, and
`production_solver_allowed` remains false.
