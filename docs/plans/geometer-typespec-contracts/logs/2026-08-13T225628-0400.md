+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T225628-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T22:56:28-04:00"
+++

# Log: exact half-edge arrangement topology feasibility

The accepted exact curve-domain predicates now feed a deterministic,
resource-governed half-edge topology builder for pre-split line and circular-
arc intervals. It deduplicates endpoints by exact `(x, y)` comparison,
canonicalizes interval direction independently of caller order, merges exact
duplicate intervals, and retains sorted occurrence membership with explicit
orientation relative to the canonical carrier direction.

Each unique interval produces one twin pair. Vertices and edges use complete
semantic ordering rather than hashes or allocation order. Outgoing half-edges
sort by exact tangent half-plane and cross-product order. Equal tangent
directions use exact signed curvature and radius order, including the positive-
X seam, so point-tangent analytic circles remain separate without sampled
offsets. The clockwise predecessor of each twin defines `next`; inverse links,
twin involution, even vertex degree, and closed cycle traversal are checked
before publication.

The builder validates its atomic-input boundary. Interior crossings,
T-junctions, partial same-carrier overlaps, invalid construction references,
and contradictory occurrence orientation fail closed. Exact duplicate domains
are permitted and merge memberships; complementary same-circle arcs sharing
only endpoints remain distinct. A complete source-declared pair count is
bounded by the governed 8,388,608 pair ceiling until certified pruning is
added. All intersection constructions live inside an outer arena transaction
and are rolled back on success or failure.

The governed vector proves permutation and authored-direction invariance for a
square, coincident membership retention, twin/next/previous closure, tangent-
circle germ ordering with two distinct interiors, unsplit crossing and partial-
overlap rejection, invalid-node rejection, and a one-byte-short storage
boundary. Its canonical signature SHA-256 is
`4232368df34ffdd21e4164dd7760288a866a04a223006875e23ebda34cf0d8b2`, the
successful work boundary is 2,876,692 units, and its retained logical storage
charge is 8,320 bytes. Native and Emscripten emit identical evidence.

This is arrangement-topology feasibility only. Face enumeration and exact
inside/outside classification, ordered Boolean stage selection, lineage,
normalization, packet projection, OCCT bidirectional audit, and production
dispatch remain disabled.

Validation passes: native CTest 22/22; governed exact Emscripten CTest 13/13;
exact-arrangement native/WASM parity; promotion-manifest tests 13/13; full Rack
77 passed with only the intentional active-plan L99 sentinel failing; Ruff;
Pyright; uv lock; clang-format; Lizard; and wn-dev-std 2026.8.12 repository,
docs.plans, and language audits. The broader legacy WASM CTest inventory is not
claimed by this slice; its maintained Rack browser-WASM stratum passes 3/3.
