+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-16T060546-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-16T06:05:46-04:00"
+++

# Log: filtered-analytic-solver-design

# Split-heavy face-selection admission remediation

Closed the remaining performance-review findings on the indexed filtered
face-selection slice.

Selection admission now bulk-precharges one fixed-work scan of canonical
broad-phase pairs and derives a conservative possible split-span and
coverage-transition capacity before arrangement begins. Line/line candidates
produce at most one point; line/circle and circle/circle candidates produce at
most two. Exact shared authored endpoints are removed from the split count.
Lowering's dense first-use carrier identities keep the coverage bound
output-linear, while noncanonical repeated carrier identities use a safe
span-by-curve bound.

A sparse 64-crossing comb fixture proves that the full solve succeeds at the
minimal canonical admission budget and fails one byte short before arrangement,
with zero arrangement work and storage telemetry. Malformed filtered endpoint
enclosures now remain `invalid_argument` regardless of the working-memory
budget. The third proportional cycle-publication traversal is precharged before
it begins.

Focused native and WASM filtered tests pass 5/5. Overlay and arrangement parity
remain unchanged. Boolean-selection native/WASM parity is exact at 12,360 bytes
and SHA-256
`0924963a9a86709e1cafad69d7178bbd2fd58204da842fb11e79453d2ebe666d`.

The complete native suites pass 45/45 in both build trees and the complete WASM
suite passes 44/44. Python passes 61/61; native runtime/examples and clean-wheel
package validation pass. Ruff, Pyright, uv lock, clang-format, focused Lizard,
diff hygiene, and the wn-dev-std repository/plan/test/language/CI/compatibility
audits are clean. The L99 release stratum remains 8/10 only for the two
pre-existing plan-wide conditions: request-packet validator CCN 113 exceeds 100
and the active temporary plan remains under `docs/plans`.

Selected-boundary publication, complete lineage projection, final 1 nm
normalization, owned production dispatch, and transport integration remain
pending. The algebraic backend remains a non-primary oracle/fallback only.
