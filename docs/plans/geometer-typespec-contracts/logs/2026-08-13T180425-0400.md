+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T180425-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T18:04:25-04:00"
+++

# Log: exact rational feasibility foundation

Opened only the independently accepted exact-backend feasibility implementation
gate. The manifest locks the accepted design revision and reviewer packet while
keeping production solver permission false.

Added a SHA-256-verifying Boost 1.92.0 restore helper and automatic CMake
bootstrap into ignored `.deps/boost_1_92_0` state. The helper validates archive
members before extraction and records the pinned upstream version, commit,
archive URL, and digest in a local provenance sentinel. The dependency remains
header-only and is not committed.

Implemented the first focused C++17 backend module: arbitrary-precision integer
and rational values, reduced-sign canonicalization, exact interning, minimal
big-endian integer magnitude encoding, rational scalar encoding, checked
storage/work reservations, rollback of semantic/live storage state, and
monotonic consumption of executed work. Tests cover values beyond 128 bits,
sign/GCD and zero normalization, equivalent-value interning, exact bytes,
invalid denominator rejection, and a completed first phase followed by a later
budget failure and retry.

The ignored Boost tree exposed a wn-dev-std 2026.8.12 plan-audit traversal gap
because upstream contains `libs/log`; the governed plans ignore list now excludes
only `.deps`, preserving source-tree rogue-plan enforcement.

Validation: native CTest passes 11/11, focused promotion-manifest tests pass
11/11, full contract freshness and 22 vectors pass, clang-format, Ruff,
Pyright, uv lock, diff checks, and wn-dev-std repository/docs.plans audits pass.
This is an implementation foundation, not completion of the polynomial,
algebraic-root, DAG, codec, malformed-vector, or Emscripten feasibility gate.
