+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-15T223639-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-15T22:36:39-04:00"
+++

# Log: filtered-analytic-solver-design

Closed the remaining independent numeric-review blocker on overlay remediation candidate `0dad3355d0e15a0d9c20e13b41f491637669220b`.

- Finite-domain repair is now certified from actual curve witnesses. When an infinite-carrier intersection lies beyond both finite curves, endpoint witnesses must be at or below 50 nm from each other; two independent sub-50-nm distances cannot compose into an above-envelope bridge. A domain interval that merely straddles an endpoint is tracked separately and fails closed if the pair witness cannot decide it.
- Added the reviewer's diagonal threshold cases: 35/35 nm endpoint offsets bridge because the real gap is below 50 nm, while 36/36 nm remains disjoint because the real gap is above 50 nm. A true intersection near both endpoints remains an ordinary point rather than a resolution collapse. Parallel 50/51 nm carriers cannot acquire an injected point.
- Removed the caller-constructible narrow-result boundary from filtered overlay. The overlay now accepts only canonical broad-phase pairs and invokes the narrow phase internally, so malformed intersection records cannot reach event sorting. Narrow predicate work and retained pair storage remain live charges in the integrated stage totals.
- Metered every endpoint-witness search and pair-distance/classification predicate. One-unit-short and one-byte-short integrated fixtures stop without output. The design, ADR, plan, manifest policies, digest locks, and native/WASM vector pins were refreshed.

Validation:
- Native CTest 43/43 and WASM CTest 42/42 passed after complete relink.
- All 22 native/WASM parity validators passed. The filtered-core vector is 264 bytes at SHA-256 `bacb4f77c3f2121df476196b42b5fe5d53b41fadce9583a4753fc87d9944e7cc`; the integrated overlay vector remains 968 bytes at SHA-256 `8f79c9c2c225cec225d6d57039388ece5d4f1c5cca47771451714ccf692fd14d`.
- Python 61/61 and manifest 15/15 passed. Ruff, Pyright, uv lock, clang-format, touched-file Lizard thresholds, git diff hygiene, and wn-dev-std repository/plan audits passed.
- L99 remains 8/10 only for the two pre-existing plan-wide conditions: request-packet validator CCN 113 exceeds 100, and the active temporary plan remains under `docs/plans`.

Production dispatch remains disabled. The next large filtered-solver slice owns global vertex reconciliation, half-edge arrangement construction, and face classification without all-pairs scans or linear duplicate searches.
