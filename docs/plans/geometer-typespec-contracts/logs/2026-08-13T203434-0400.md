+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T203434-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T20:34:34-04:00"
+++

# Log: scalar decoder canonicality and GCD remediation

Independent review found that the first decoder accepted extra whole alignment
blocks, normalized scaled polynomial presentations, and duplicated rational GCD
work before the charged canonical factory. Both scalar branches now require
`input.size() == align(payload_end, 8)` and validate only that minimal padding.
Irrational decode compares the encoded coefficient list byte-semantically to
the reconstructed primitive positive-leading irreducible polynomial.

Rational decode now delegates normalization to the conservatively charged
factory and rejects unless the returned numerator and denominator exactly equal
the parsed pair. The negative 137-bit fixture succeeds at exactly 984 decode
work units; a 983-unit budget consumes the 768-unit parse phase, fails before
normalization, and rolls all temporary storage back identically in native and
Emscripten. New mutations reject adjusted-length extra padding and a scaled
`sqrt(8)` polynomial presentation.

The full construction DAG/artifact, expression arithmetic, and production
integration remain gated pending independent acceptance.
