+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T201215-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-planar-boolean-promotion"
created = "2026-08-14T20:12:15-04:00"
+++

# Log: analytic-planar-boolean-promotion

# Log: analytic planar Boolean promotion opened

With `occt-8-0-1-qualification` closed and its published caches independently
accepted at `ea26d68b58ed57e43509ae24fb55c501048e322d`, the
`analytic-planar-boolean-promotion` step is now active. This opening slice
changes no contract sources or product code; it records the ordered slice
sequence the executor will follow so each transition receives its own
independent review.

Current state feeding this step: the analytic TypeSpec sources compile in
isolation behind `analytic-candidate.tsp` and are excluded from the emitted
main catalog; the generated catalog carries only model-bounds, outcome,
diagnostic, and IPC surfaces; the accepted analytic design and its
`design_sha256` are locked in `docs/contracts/promotion-manifest.toml`; MATZ
portable observations exist under
`tests/fixtures/analytic_planar_boolean/matz_observations_a0.json` with the
named consumer `appz.data_models.pcb.matz`; and
`production_solver_allowed = false`.

Planned promotion slices, each committed and reviewed separately:

1. Contract freeze: import the analytic operation into the main TypeSpec
   catalog, regenerate the catalog, schemas, and HTML reference
   deterministically, and pass candidate, hygiene, and generation gates.
2. Generated C++ server integration for the analytic operation with shared
   conformance vectors across the C++ lanes.
3. Generated TypeScript/WASM client integration with native/browser canonical
   parity evidence.
4. Generated Rust executable IPC client integration with live pipe smoke.
5. Generated Python public-API integration with wheel round-trip proof.
6. Production dispatch switch, frozen MATZ fixture replay, displaced-code
   deletion, and digest-checked manifest promotion. Only this slice may
   propose flipping `production_solver_allowed`, and it requires its own
   explicit review before any dispatch change lands.

The interactive demo and focused additive release remain governed by the
separate `analytic-planar-boolean-demo-release` step. Commits stay local-only
per standing human direction.
