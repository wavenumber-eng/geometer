+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T081500-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T08:15:00-04:00"
+++

# Log: exact boundary and vertex provenance

Exact result boundaries now project catalog-aligned source-reference tuples for
every retained analytic half-edge. Coincident positive operands remain distinct
contributors, and subtraction history on the empty side of a boundary is
retained as a governed subtractive-effect reference even when a later union
refills material. Result vertices independently carry the canonical union of
all exact source occurrences incident at that arrangement vertex.

The projection validates complete occurrence metadata, coverage/operand
closure, unique nonzero identities, source kind and role values, compact
feature boundary-occurrence-key structure, and agreement between each source
role and its line or circular-arc carrier. Malformed ranges, indices, tuples,
and metadata fail closed. Source and occurrence lists are sorted independently
of input metadata order, and resource preflight bounds both work and logical
storage before a result is exposed.

The closed-form fixture combines two coincident positive outer boxes, a
subtractive inner box, and a later positive refill island. It proves coincident
positive boundary contributors, hole subtraction effects, refill boundaries
that preserve both positive and prior subtractive provenance, complete
four-source outer vertices, deterministic metadata permutation, invalid tuple
rejection, curve-role coherence, and one-unit/one-byte fail-closed boundaries.

The native/Emscripten canonical signature SHA-256 is
`3e996f29bfeca09f584dc6412670302d92edbd4f3282bc921829b23dd79108e5`.
The fixture boundary is 4,208 work units and 26,528 conservatively reserved
logical bytes.

Independent review identified two cross-artifact closure gaps. The remediated
builder now proves that the region rings contain every and only material/empty
selection boundary half-edge, with no unreferenced ring entries, and recomputes
the ordered stage semantics for every arrangement face before accepting the
selection's material and lineage ranges. Regressions pair a nonempty selection
with a separately valid empty region projection and relabel valid union stages
as differences; both mismatches now fail closed.

This remains exact feasibility infrastructure, not the production operation.
Governed operand outcomes, final certified-nm normalization and packet
encoding, the OCCT audit, broader synthetic/property/mutation coverage, and
production dispatch remain pending; `production_solver_allowed` remains
false.

Validation passes: native validation and CTest 25/25; governed exact
Emscripten build and native/WASM provenance parity; promotion-manifest tests
13/13; Ruff; Pyright; uv lock; clang-format; Lizard; and wn-dev-std 2026.8.12
repository, docs.plans, and language audits. Full Rack reports 77 passed with
only the intentional active-plan L99 sentinel failing.
