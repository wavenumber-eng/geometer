+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T132041-0400"
plan_id = "geometer-typespec-contracts"
step_id = "rust-exe-ipc"
created = "2026-08-13T13:20:41-04:00"
+++

# Log: rust-exe-ipc

Implemented the first reviewable Rust/executable IPC A0 vertical without
closing the owning plan step. Added the native binary frame codec and
`geometer serve --stdio`, serialized/bounded request execution, queue-only
cancellation, ordered graceful shutdown, generated strict Rust operation DTOs,
the Tokio persistent client, exact framing and governed-vector conformance, a
real repeated STEP model-bounds process test, and clean packaged-crate consumer
validation.

The slice also hardened the TypeScript Worker client so a correlated but wrong
response kind fails the whole connection. Rust vector replay found and closed
an explicit-null presence bug in the initial generated Serde projection.

Validation at this slice: native CTest 10/10, focused Python/TypeSpec/docs/WASM/
TypeScript/Rust 53/53, governed vectors 20/20 in Rust plus the existing lanes,
Clippy with warnings denied, verified `cargo package`, Ruff, Pyright, uv lock,
contract/package/documentation freshness, and diff checks pass. Rack reports 61
passed and one intentional L99 failure because this active temporary plan is
still present.

The plan step remains pending for generated TypeSpec transport-control DTOs,
the remaining fatal framing/broken-pipe/forced-deadline/unexpected-exit tests,
and the native multi-platform artifact matrix. No release or promotion is
claimed by this intermediate implementation.

The project and installed validator were upgraded to `wn-dev-std` 2026.8.12.
The TypeScript package moved into the profile-compliant grouped path
`dist/wasm/npm/geometer`, and every source, example, test, manifest, and guide
now uses that location. The Rust crate carries its own 2026.8.12 `rust-app`
profile with stable rustfmt/Clippy components, forbidden unsafe code, canonical
denied Clippy lints, declared Cargo signoff commands, and strict Tree-sitter
source-shape limits. The root repository, root language, docs.plans, and nested
Rust language audits pass without exceptions.
