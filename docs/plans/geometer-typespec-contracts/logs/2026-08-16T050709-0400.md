+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-16T050709-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-16T05:07:09-04:00"
+++

# Log: filtered-analytic-solver-design

# Face-selection exact-commit review remediation

Remediated the independent numeric, integration, and performance findings on
the indexed filtered face-selection slice.

Circle overlay clusters now retain ordered source supports separately from
their published representative. Synthetic left/right seam points sort first at
their exact partition, but no event is independently snapped to them. Every
sequential and cyclic merge certifies the complete support set at or below the
50 nm envelope. A disk of radius 1000 nm intersected by `x = 999 nm` therefore
keeps its two approximately 89.4 nm-separated roots, while correlated
constructed capsule seams still normalize without algebraic fallback. The
integrated regression also proves face selection preserves every arrangement
coordinate.

The selection result now carries both lowering origin coordinates, including a
large negative-origin regression, so publication can reconstruct global nm
coordinates without retaining or rescanning the request.

Resource remediation prebinds each occurrence to a dense operand ordinal,
making membership-to-transition construction O(M). Every admission and builder
stage-table pass is bulk-charged before traversal. Known-size operand, column,
face, boundary, temporary sweep, and DFS vectors reserve canonical capacities;
logical memory reports the maximum actual live phase rather than the sum of
sequential phases. An allocation-free certificate recognizes monotonically
separated short carriers and reserves their unavoidable collapsed vertices and
sort work before arrangement execution. Native/WASM tests cover 257 zero-
operand stages, 257 operands with 1,028 memberships, non-power-of-two exact and
one-byte-short capacity, 1x/2x sparse scaling, and early rejection with zero
arrangement telemetry.

Validation: native CTest passes 45/45 in both native trees; WASM CTest passes
44/44. All 24 parity validators pass. Updated canonical vectors are overlay
1,048 bytes / `8c1162b7e14b6a58a2dd8f50c4c4b87767a223632c6393a3a8ace3d0244ac352`,
arrangement 13,032 bytes /
`1ac528f9b7f45b1a36fad5f0fafb20b96c6bee8c0ad6e76e0c758783b3d770b3`,
and Boolean selection 12,360 bytes /
`af7167919a461cd69b7f055cbc609a933bd1248ad47bba7834ee6b47a12a9806`.
Python passes 61/61 and the promotion manifest passes 15/15; Ruff and Pyright
are clean. Rack reports 88 passing tests and the same two plan-wide release
conditions: the pre-existing request-packet validator remains at CCN 113
against the release threshold of 100, and the active temporary plan remains
under `docs/plans`. The repository, tests, language, CI, compatibility, and
plan-log wn-dev-std audits pass.

Selected-boundary publication, complete lineage projection, final 1 nm
normalization, owned production dispatch, and transport integration remain
pending. The algebraic backend remains a non-primary oracle/fallback only.
