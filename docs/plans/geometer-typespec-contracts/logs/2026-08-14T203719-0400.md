+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T203719-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-planar-boolean-promotion"
created = "2026-08-14T20:37:19-04:00"
+++

# Log: analytic-planar-boolean-promotion

# Promotion slice 1: analytic planar Boolean contract freeze

Imported the frozen analytic-planar-boolean operation TypeSpec into the main
catalog entry point and regenerated every governed contract artifact
deterministically.

## Changes

- `src/tsp/geometer/main.tsp` now imports
  `operations/analytic-planar-boolean-a0.tsp` and
  `operations/analytic-planar-boolean-operation-a0.tsp`; the candidate
  entrypoint compiles to the identical catalog, so the isolation checker
  passes unchanged.
- Normalized catalog gains the operation
  `geometry.analytic_planar_boolean_batch.a0` and roots
  `geometry.analytic_planar_boolean_batch.request.a0` /
  `geometry.analytic_planar_boolean_batch.result.a0`; new JSON Schemas and
  generated HTML reference pages emitted. New catalog SHA-256
  `126edc93c7fbb23b0e15da35966abe0145972586807be6b7fdbc46948adb175c`
  re-pinned in the promotion manifest and embedded in the regenerated C++
  runtime (`normalized_contract_catalog_sha256`) and Rust
  (`NORMALIZED_CATALOG_SHA256`) projections (one-line diffs each).
- Language projections stay deferred to their implementation slices via the
  new manifest field `deferred_projections = ["cpp", "typescript", "rust",
  "python"]` on the candidate record, enforced by the shared
  `scripts/contract-projection-deferral.mjs` filter in all four generators;
  generated C++/TS/Rust/Python DTO surfaces are byte-identical apart from the
  catalog digest, and the runtime operation catalog still advertises exactly
  `geometry.model_bounds.a0`. Unknown operations keep failing closed with
  `geometer.contract.unsupported_operation`.
- Promotion manifest: candidate status `design_frozen` -> `contract_frozen`,
  request/result contracts now reference the real catalog identities, the
  stale implementation gate is now
  `cpp_wasm_rust_python_implementation_slices_pending`, and the two frozen
  contract roots have `candidate_frozen` lifecycle entries.
- `scripts/generate-contracts.mjs` validates frozen candidate operations in
  the catalog; `scripts/generate-contract-docs.mjs` sources operation
  lifecycle from `[[candidate_operations]]`;
  `scripts/check-contract-vectors.mjs` registers the analytic schema
  extension keywords and disables only Ajv's strictTypes lint (the governed
  int64-as-string schema strategy keeps numeric bounds that the lint rejects
  on string-typed scalars; bounds remain governed by catalog constraints).
- Rebuilt native and WASM; only the native dist binaries change (embedded
  catalog digest for IPC welcome). Browser `geometer.js`/`geometer.wasm` are
  byte-identical, so their evidence pins are unchanged.

## Gates

- `npm run check:contracts` green (all projections current, biome clean,
  22 governed vectors validated, analytic candidate valid and isolated).
- Native ctest 38/38, WASM ctest 37/37.
- `python -m pytest tests/python -q` 55 passed;
  `python -m pytest tests/contract_docs -q` 3 passed; ruff clean.
- `production_solver_allowed` remains `false`; no dispatch change.
