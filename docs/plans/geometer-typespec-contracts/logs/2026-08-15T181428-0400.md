+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-15T181428-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-15T18:14:28-04:00"
+++

# Log: filtered-analytic-solver-design

# Cross-runtime working-memory remediation

The second independent foundation review accepted the numeric classifier, examined-pair budget, interval-index complexity, radix ordering, and cache corrections but found that memory telemetry still depended on ABI layout and could undercount priority-queue capacity.

The broad phase now charges one fixed conservative 96-byte logical sweep record per curve on every target: an 8-byte order slot, a maximum 56-byte interval node, and a 32-byte expiry entry. Compile-time guards reject a supported ABI whose structures exceed those charges. Interval nodes and expiry entries use fixed-capacity arrays, eliminating vector-capacity growth beyond telemetry. A 257-curve non-power-of-two boundary test succeeds at exactly the canonical charge and fails one byte below it under both native and WASM.

Validation: native CTest 41/41; WASM CTest 40/40; focused promotion/cache pytest 36 passed; clang-format, manifest hash, diff, and standards checks passed.
