+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T100600-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T10:06:00-04:00"
+++

# Log: strict analytic result-packet layout codec

The first raw-byte result-packet layer now encodes and decodes the fixed A0
header, all fourteen required result directory entries, exact fixed-record
lengths, canonical payload accounting, and minimum-aligned table placement.
The decoder reads every scalar explicitly in little-endian order, performs all
size arithmetic before range access, accepts at most 256 MiB, requires the
unique directory/offset layout, and rejects truncation, trailing data,
overlap/noncanonical offsets, multiplication mismatches, header count drift,
job/relationship count limit violations, and nonzero padding or reserved
header fields.

Implementation exposed a contradiction in the frozen prose before any codec
or raw golden existed: exact four-byte table lengths for kinds 106 and 114 can
be four modulo eight, while the old scalar rule also required every table byte
length to be eight-byte aligned. Independent reviewer packet
`reviewer-01a00092-b5d6-7022-95f4-5aa60b7f789a` accepted the uniquely encodable
A0 erratum: offsets remain eight-byte aligned, lengths remain exactly
`count * record bytes`, and minimum zero padding aligns the following table.
Generation 1 and `GMABRS01` remain unchanged. The corrected packet-spec digest
`2a3264411e07829904847b5334897cc3da95e07482355a468964159428b24155` is
recorded in the promotion manifest before byte goldens freeze.

The unique empty layout is 512 bytes with SHA-256
`6098488722c00c2959df6e16d1381cbdcacf79c5bd4d8b9ae2cebdd5a0de8638`.
The 924-byte all-table layout vector includes one record of every governed
size, an odd four-byte kind-106 table, and its exact four zero padding bytes;
its SHA-256 is
`4bce719a8da4fe334ea1bc758a7d726a77deec90976aab62b81e205e4a3f493c`.
Native and Emscripten bytes agree exactly.

This layer validates packet layout only and deliberately does not claim typed
record semantics, structural graph closure, canonical logical ordering,
standalone job rebasing, or SHA-256 job digests. Those remain the next codec
slices. Production dispatch remains disabled and `production_solver_allowed`
remains false.

Validation passes: native CTest 29/29; native/Emscripten byte parity for both
layout goldens; promotion-manifest tests 13/13; isolated analytic TypeSpec
candidate check; Ruff; Pyright; uv lock; clang-format; Lizard; diff checks; and
wn-dev-std 2026.8.12 repository, docs.plans, and language audits. Full Rack
reports 77 passed with only the intentional active-plan L99 sentinel failing.
