+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-16T132230-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-16T13:22:30-04:00"
+++

# Log: filtered-analytic-solver-design

# Add one-time filtered 1 nm normalization and strict topology replay

Added an owned normalization continuation from request records, trusted
filtered geometry, and canonical broad-phase pairs. Used material-boundary
vertices are rounded once on the global integer-nanometer grid with exact
binary64 expansion comparisons at half-grid ties, then certified against their
complete arrangement hull. Lines preserve endpoint correspondence. Circular
arcs reconstruct only an ephemeral filtered center and certify both directed
Hausdorff bounds from the finite circle/line critical set using outward
interval and squared-distance comparisons, avoiding canonical nested-radical
storage and every algebraic fallback.

The normalized line/arc graph is replayed through strict candidate discovery
and zero-repair intersection checks. A fresh material-region build must match
the original boundary partition, winding, parent/depth hierarchy, and outer
region ownership through direct boundary-index maps. Carrier grouping and
topology comparison are sorted/indexed; there is no fragment-pair, ring-pair,
or face-by-operand scan. The result retains explicit old vertex, boundary,
ring, and region maps for the next packet-assembly slice. Work and logical
memory are reserved before outcomes execute, and exact/one-short plus sparse
1x/2x tests gate the phase.

Native CTest passed 49/49. All 48 WASM tests passed (44 in the parallel pass;
the four OCCT projection/operation executables that cannot instantiate
concurrently passed serially). Nine filtered native/WASM parity validators
passed; normalization is 1,968 bytes at SHA-256
`899d588c79a4228ab4798ad9e14995850a6cc76f36238affbc8ee81767beb4c8`.
Promotion-manifest pytest passed 15/15, Ruff and diff hygiene passed. Governed
native archives are identical at 45,500,790 bytes and SHA-256
`315526e20add927104f911551cac03bf97d543262852cb6d342d024ac220dedb`.

The algebraic solver remains a non-primary oracle and production dispatch is
still disabled. Canonical source-set interning, packet record ordering/IDs,
job diagnostics/digest, dispatch, and transport remain pending.
