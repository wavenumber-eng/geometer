[project]
name = "geometer-python-examples"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pyside6",
    "pyvista",
    "pyvistaqt",
    "trimesh",
    "wn-geometer",
]

[tool.uv]
package = false

[tool.uv.sources]
wn-geometer = { path = "../..", editable = true }
