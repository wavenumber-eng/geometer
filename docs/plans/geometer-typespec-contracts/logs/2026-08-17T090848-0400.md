+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-17T090848-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-planar-boolean-promotion"
created = "2026-08-17T09:08:48-04:00"
+++

# Log: analytic-planar-boolean-promotion

# Generated transport, TypeScript projection, and interactive demo checkpoint

Closed the production transport and first TypeScript analytic vertical. The
TypeSpec IPC request is now an authoritative generated one-of union for logical
DTO and packed-attachment requests; the handwritten analytic pre-decode bypass
is removed. Generated operation metadata owns packed request/result contracts,
projections, media types, attachment limits, and logical result-variant
matching. Native response validation fail-closes malformed outcomes and binds
operation, schema, projection, attachment identity, format, success/failure
attachment policy, and required output presence.

The `@wavenumber/geometer` package now projects `int64` and `uint64` as
`bigint`, exports the analytic request/result surface, and provides direct and
Worker `analyticPlanarBooleanBatch` methods. Its strict packed A0 codec enforces
the native packet invariants, canonical order and byte encoding, result count
ceilings, ownership, topology, provenance, diagnostics, relationships, and
standalone per-job digests. Independent TypeScript review replayed semantic
fragment-order and invalid-source mutations, verified failed-job event parity,
and accepted the linear owner/selection closure plus selection-sized sparse
rebasing maps. Full-packet canonical re-encoding still creates peak memory near
the 256 MiB packet ceiling and remains a nonblocking optimization follow-up.

All production transports now execute nonempty analytic work: the generic
native C ABI returns a decoded single-disk result, browser WASM and the real
Worker client return the same logical closure, and the Rust persistent IPC
client sends a deterministic packed disk request and validates a nonempty
result attachment. Shared C++/TypeScript result vectors prove canonical packet
and mixed/standalone digest parity.

Added the TypeScript Worker polygon-pour demonstration. Its shared typed fixture
solves two disconnected regions containing four circular holes, eight line and
eight circular-arc boundary fragments, duplicate-source lineage, and a
neighboring job-local `unsupported_geometry` failure. The center fixture pins
the exact topology, outcome inventory, and standalone digest. Latest-only
scheduling prevents Worker backlog and stale presentation. Canvas arc branch
resolution covers reflected clockwise/counterclockwise minor, major,
semicircle, and near-diameter cases. The deterministic 33-file static directory
includes the generated package, Worker, browser WASM, local font, CSP/MIME
headers, recursive import-closure validation, and SHA-256 asset manifest.

Fresh locked artifacts:

- browser JS: `a60ef4f4007fae9c8be7cce9ee55c7f869f8e30cba3636174845c03aad57b9a9`
- browser WASM: `4f52835b7fcccdb56a1fcd2ef6699fb5cd4c63e0c8eb01051304f9edfad1a981`
- analytic static site: `857b152e32cfadd7aee97458bff2487a77927de60574e4d99a093335e7f61c83`

Local validation passes: capped four-job native and WASM builds; 51/51 CTests
at two-way test parallelism; 9/9 TypeScript/WASM validations; 7/7 Rust IPC
tests at four build jobs and two test threads; 15/15 promotion-manifest tests;
22 governed contract vectors; deterministic generator/check mode; Ruff; Biome;
clang-format; and diff hygiene. The WASM rebuild remained bounded near 25 GiB
total system use, confirming that explicit default parallel caps prevent the
prior all-core compiler fan-out.

Headless Chrome then booted the exact static directory at desktop and 390 px
widths. The desktop run completed the initial solve, performed a real slider
drag, and returned to ready with two regions, 16 exact fragments, and the
governed neighboring failure; observed round trips were 48.9 ms cold and 7.2
ms after the drag. The narrow pass found and closed a 432 px diagnostic-driven
min-content overflow, then verified a 390 px document/canvas with no horizontal
overflow. Both rendered captures were inspected. The analytic browser-demo exit
criterion is met; hosted preview and focused release publication remain later
steps.

The filtered solver design step is complete and analytic promotion is active.
Remaining promotion/release work includes the deferred logical DTO projections
and public Python compatibility boundary, representative consumer/MATZ replay,
real desktop/narrow browser presentation smoke, hosted preview/publication,
dense performance evidence, and the full Rack/L99 release signoff.
