+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T232137-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T23:21:37-04:00"
+++

# Log: exact face enumeration and bounded-set classification

The accepted half-edge topology now enumerates canonical directed boundary
cycles, connected arrangement components, and true planar faces. Each
connected component has one exact clockwise exterior cycle; counterclockwise
cycles create bounded faces. Exact line/circle ray predicates place exterior
cycles from nested disconnected components into the smallest containing face,
so the unbounded face, annuli, and nested holes have explicit deterministic
boundary-cycle lists. Point-tangent components remain connected
combinatorially and retain distinct bounded faces.

Cycle orientation uses exact outgoing germs at the lexicographically least
vertex. If a major circular arc reaches a smaller carrier-leftmost point in
its open domain, its exact directed circle orientation governs instead. No
sampled chord, angle, area approximation, hash, allocation identity, or OCCT
candidate participates in face identity or containment.

Optional coverage metadata maps every source occurrence to one bounded
coverage id and declares which side of its own direction contains material.
The classifier converts carrier-relative occurrence direction to edge-relative
material side, propagates sorted coverage sets from the empty unbounded face
through the exact dual graph, and verifies every edge-side constraint.
Missing, unused, duplicate, or incoherent coverage metadata fails closed.
Input order does not affect face ids, boundary lists, or coverage sets.

The governed vector now includes cycle/face identities and square coverage.
Closed-form regressions separately cover point-tangent disks with distinct
coverage, a nested rectangular hole whose annular face owns two boundary
cycles, and a major-arc region whose true leftmost point lies inside the arc.
Its canonical signature SHA-256 is
`b134a245f7d3daa6d4f8e03db1871ec8e99565a500e87ba4c452090951046c23`, the
successful work boundary is 3,051,260 units, and conservatively reserved
logical storage is 8,608 bytes. Native and Emscripten emit identical evidence.

This remains exact arrangement feasibility. Ordered union/difference stage
selection, positive and subtractive lineage, final normalization and packet
projection, OCCT bidirectional audit, production dispatch, and the broader
synthetic/property/mutation corpus remain disabled or pending.

Validation passes: native CTest 22/22; governed exact Emscripten CTest 13/13;
exact-arrangement native/WASM parity; promotion-manifest tests 13/13; full Rack
77 passed with only the intentional active-plan L99 sentinel failing; Ruff;
Pyright; uv lock; clang-format; Lizard; and wn-dev-std 2026.8.12 repository,
docs.plans, and language audits. The maintained browser-WASM Rack stratum also
passes 3/3.
