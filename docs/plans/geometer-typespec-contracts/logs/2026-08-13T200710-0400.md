+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T200710-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T20:07:10-04:00"
+++

# Log: canonical exact-real value and comparison feasibility

Built canonical scalar values on the independently accepted factor/root layer.
A selected degree-one factor collapses to a reduced rational with positive
denominator. A selected higher-degree irreducible factor is reconstructed as a
primitive polynomial and paired with its canonical smallest dyadic interval,
root ordinal, and exact Thom signs. Ambiguous source intervals preserve the
non-error `needs_refinement` outcome.

Exact equality uses canonical minimal-polynomial coefficients plus root ordinal.
Same-polynomial order uses root ordinal; irrational/rational order and sign use
exact Sturm counts at the rational boundary; different-polynomial order refines
both dyadic enclosures until disjoint or the governed ceiling/resource budget
is reached. Rational comparison uses checked, precharged cross multiplication.
All public construction and comparison paths contain allocation/estimate
exceptions and return typed failures.

Native and Emscripten tests cover rational zero collapse, positive and negative
irrational identity/sign, rational/irrational order, same-polynomial order,
cross-polynomial `sqrt(2) < sqrt(3)`, exact equality, ambiguity/refinement, and
storage exhaustion.

Construction DAG normalization, canonical value-key/artifact codecs, malformed
vectors, exact arithmetic expression evaluation, and production solver
integration remain gated.

Validation passes all 15 native CTests, all five exact Emscripten tests, the 12
promotion-manifest tests, Ruff, Pyright, uv lock, clang-format, complexity,
repository audit, and docs.plans audit. Rack reports 71 passing tests and only
the intentional release-hygiene sentinel caused by this active temporary plan.
