+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T154500-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T15:45:00-04:00"
+++

# Log: deterministic seeded-property failure reduction

The seeded property harness now reduces any boundary, component-count, or
same-stage operand-reversal failure before terminating. Its deterministic
greedy reducer removes stages and operands, then shrinks and translates
rectangle extents while requiring the same property identity to remain
failing. The diagnostic includes both the original seed/case/replay descriptor
and the resulting one-minimal descriptor under those transformations.

An injected sentinel proves the reducer path by collapsing a three-stage,
four-rectangle fixture to `U[0,0,1,1]`. The reducer sentinel and full actual
boundary/lineage projection are governed under native/Emscripten SHA-256
`d201a374b0816f7e29f18ad1fb17314f2f3ac9b3a43e62a7d57a7f8250b66b97`.
Real failures must still be promoted as committed regression fixtures after
investigation. Production solver dispatch remains disabled.
