+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T153700-0400"
plan_id = "geometer-typespec-contracts"
step_id = "python-public-contracts"
created = "2026-08-13T15:37:00-04:00"
+++

# Log: generated Python compatible boundary

Added a deterministic Python generator for the complete normalized catalog.
It emits frozen slotted dataclasses, enums, union aliases, fixed tuples, and
strict codecs under the internal `geometer._generated.contracts` namespace.
The standard-library runtime rejects invalid UTF-8, trailing JSON, duplicate
and unknown fields, null option values, non-finite or out-of-range numbers,
invalid literals/enums, ambiguous unions, and array/string bound violations.
No runtime dependency, public generated-model exposure, Python-version floor,
or package identity changes were introduced.

The existing public `model_bounds` and `model_bounds_json` signatures now pass
their canonical request and result through generated codecs. Their
compatibility adapter retains `model_format`, `modelTransform`, uppercase
`STEP`, nested 4-by-4 transforms, and ignored legacy mapping members while
ensuring none reach the closed canonical DTO. The existing public
`ModelBoundsResult`, executable discovery, backend selection, and all other
file-oriented CLI operations remain unchanged.

Python replays all 20 governed contract vectors and adds focused strict-codec,
presence, diagnostic-path, live model-bounds, and compatibility-alias tests.
Generated source is included by normal setuptools discovery. A clean Windows
wheel builds, installs into a fresh environment, imports the internal generated
codec, resolves its bundled executable, and completes a validated live
`model_bounds` call plus the existing projection, GLB, planar STEP, batch, CLI,
and example checks. All 42 Python tests, Ruff, Pyright, generation freshness,
and 20 governed vectors pass. Full Rack reports 65 passed and the one expected
active-plan release-hygiene failure. The step remains pending for independent
review; no model-bounds promotion or release is claimed.

Independent review added one adversarial correction: conversion of a valid
JSON integer larger than finite float64 range could raise raw Python
`OverflowError`. The shared runtime now guards conversion and reports
`geometer.contract.number_range` at the exact element pointer. Decoder and
encoder regressions cover a 400-digit matrix value, so malformed wire data and
directly constructed generated models share the governed failure behavior.
