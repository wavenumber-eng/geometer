+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T202953-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T20:29:53-04:00"
+++

# Log: strict canonical scalar decoding feasibility

Added bounded, fail-closed decoding for the accepted scalar grammar. The parser
precharges checked work and logical storage before processing variable fields,
enforces exact record length, kinds, reserved bytes, coefficient counts,
integer signs, minimal big-endian magnitude, four/eight-byte zero padding,
positive reduced rational denominators, precision ceilings, Thom counts, and
the closed signed-byte alphabet.

Rational values are reconstructed through the canonical rational factory.
Irrational values are primitive-normalized, deterministically refactored to
prove irreducibility, isolated again, and accepted only when ordinal, smallest
dyadic interval, and complete Thom evidence exactly match the encoded claim.
Tests round-trip rational and irrational goldens and reject nonzero reserved or
padding bytes, wrong lengths, nonminimal integers, reducible polynomial claims,
inconsistent Thom signs, and unreduced rationals in native and Emscripten.

The construction DAG/full `GEXPA001` artifact, expression arithmetic, and
production integration remain gated.

Validation passes all 16 native CTests, all six exact Emscripten tests, the 12
promotion-manifest tests, Ruff, Pyright, uv lock, clang-format, complexity,
repository audit, and docs.plans audit. Rack reports 71 passing tests and only
the intentional release-hygiene sentinel caused by this active temporary plan.
