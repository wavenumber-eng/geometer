+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-16T074604-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-16T07:46:04-04:00"
+++

# Log: filtered-analytic-solver-design

# Filtered material regions and early capacity admission

Implemented the owned filtered material-boundary stage from ordered request
records, trusted lowered geometry, and canonical broad-phase pairs. It selects
material-left/empty-right half-edges, assigns successors in one certified
rotation pass, traces every boundary edge once, joins equal-material faces with
an indexed disjoint set, and derives canonical ring parentage and distinct
interior-connected regions through one component-graph traversal. There is no
repeated seam walk, cycle-pair containment, point-in-ring query, coordinate
change, or second 50 nm repair.

Selection admission now reserves downstream region work and memory before
arrangement. The existing `O(curves + candidates)` scan builds a private
capacity envelope for possible narrow records, overlay events/outputs, and
arrangement phases. Exact/one-byte-short fixtures prove zero-arrangement early
rejection for a 64-edge ring and endpoint-coincident full-circle domain. A
production-lowered adjacent-rectangle union produces one six-edge exterior and
no material/material seam. Repeated disk carriers remain linear.

Numeric, performance, and integration reviewers accepted the slice. Native
CTest passes 46/46 in both MSVC trees; WASM passes 45/45; region parity is 800
bytes at SHA-256 `4a9037d938d8263a2c749a60f1f88f626c67986dfc554bbf131fd6a836834f59`.
Python passes 61/61, manifest tests 15/15, and maintained Rack strata 88 tests.
Ruff, Pyright, uv lock, clang-format, diff hygiene, and wn-dev-std audits pass.
L99 remains 8/10 only for the accepted CCN-113 request validator and active
temporary plan.

Lineage projection, one-time 1 nm normalization, post-normalization topology,
production dispatch, and transport remain pending. Algebraic exact remains a
non-primary oracle/fallback only.
