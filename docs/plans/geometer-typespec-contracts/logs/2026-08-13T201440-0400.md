+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-13T201440-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-13T20:14:40-04:00"
+++

# Log: canonical value invariant and limb-accounting remediation

Independent review found that the value-layer work estimate counted Boost
backend limbs rather than the governed 32-bit units. It now derives magnitude
bit length from the backend representation and converts it into checked 32-bit
limb counts. A 40-bit rational fixture succeeds with exactly 25,627 work units
and fails at the same completed phase with a 25,626-unit budget on both native
and Emscripten, proving the platform boundary directly.

`PolynomialFactorSet` construction is now private to the verified deterministic
factorization function, so callers cannot publish an arbitrary reducible
polynomial as a canonical minimal polynomial. The canonical `sqrt(8)` fixture
also asserts the stored smallest dyadic interval `(2,3)` at precision zero and
the complete `{+1,+1}` Thom-sign sequence, in addition to factor and root
ordinal.

Native and Emscripten value tests pass. Canonical DAGs, codecs, expression
arithmetic, and production integration remain gated pending independent
acceptance.

All five exact tests pass independently in native and Emscripten builds. The 12
promotion-manifest tests, Ruff, Pyright, clang-format, complexity, repository
audit, docs.plans audit, and diff checks also pass.
