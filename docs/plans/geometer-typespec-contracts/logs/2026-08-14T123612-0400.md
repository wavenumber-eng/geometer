+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-14T123612-0400"
plan_id = "geometer-typespec-contracts"
step_id = "analytic-synthetic-correctness"
created = "2026-08-14T12:36:12-04:00"
+++

# Log: first governed metamorphic invariant lane

The OCCT-independent closed-form corpus now exercises five metamorphic
properties over both the mixed line/semicircle capsule and nested rectangular
outer/hole/island packet: integer translation, exact 90-degree rotation,
reflection with winding recanonicalization, positive integer scaling, and
source-ID renaming.

Every transformed packet must pass exact topology replay and its independent
rational-plus-pi Green measure. Translation, rotation, reflection, and source
renaming preserve the measure; scaling by three multiplies area coefficients by
nine and perimeter coefficients by three. Reflection reverses ring traversal
after reflecting coordinates so outer/hole winding remains canonical. Source
renaming changes canonical packet identity while leaving geometry and topology
unchanged.

The ten transformed canonical packet digests form a governed aggregate
signature. Native and Emscripten must match that signature exactly. This closes
the first transformation subset, not identity/self-operation properties,
general-arc transforms, degeneracy sweeps, exhaustive/property lanes, or
production solver execution. Production dispatch remains disabled.

Validation passes: Windows native validation and CTest 32/32; focused
Emscripten build and native/Emscripten closed-form plus metamorphic digest
parity; promotion-manifest tests 14/14; Ruff; Pyright; clang-format; Lizard;
generation/package freshness; uv lock; and wn-dev-std 2026.8.12 repository,
docs.plans, root-language, and nested Rust audits. Full Rack reports 78 passed
with only the intentional active-plan L99 sentinel failing.

Independent review found that topology, aggregate measure, and a changed digest
did not alone prove that each named map was applied. The remediated test now
asserts exact field relations before those downstream oracles: translated,
rotated, reflected, and scaled coordinates; scaled radii; stable directed curve
data; reflection's reversed ring references and endpoints with net-preserved
arc direction; unchanged hierarchy/provenance tables; and the explicit
operand/primary/secondary source-ID map with identical geometry fields.
