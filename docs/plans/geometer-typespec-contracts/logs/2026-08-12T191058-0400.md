+++
type = "plan_log"
id = "independent-design-review-remediation"
plan_id = "geometer-typespec-contracts"
step_id = "transport-design-review"
created = "2026-08-12T19:10:58-04:00"
+++

# Log: independent design review remediation

Independent review at Geometer revision `2129dca` returned changes-required
for two IPC shutdown races and the analytic Boolean exact-solver, packet,
hierarchy, provenance, and mathematical closure. The generic C ABI itself had
no blocking finding. Transport implementation remains disabled.

The revised IPC A0 specification gives every queued request a normal terminal
shutdown-rejection response with one stable retryable diagnostic, FIFO
ordering, and an exact request-id transition. One monotonic 30-second deadline
now covers active execution, bounded writer submission, partial writes,
flushes, and `shutdown_ack`; final flush and expiry share one synchronized
winner, and blocked output forces nonzero termination.

ADR-012 and the analytic design now make an independently enumerated exact
line/circle half-edge arrangement authoritative. OCCT is an audited candidate
and acceleration source only. Portable real algebraic values use integer
defining polynomials, dyadic isolating intervals, Thom encodings, and exact
resultant/subresultant/GCD/root-isolation decisions over
`boost::multiprecision::cpp_int`. Deterministic combinatorial, algebraic,
predicate-work, provenance, and memory limits fail closed.

The packet requires every request/result table entry even when empty, defines
the unique zero-table offset, adds direction to the complete fragment key,
defines total result keys, makes ring parent/depth the sole hierarchy, removes
orphan query-scoped diagnostics, and closes every source-kind id and swept-path
boundary occurrence. Exact operand-event truth conditions, regularized set
semantics, zero-operand stages, degenerate/swept-path rules, containment, and
boundary-contact semantics are now normative. Diagnostic identities use the
common `geometer.operation.*` namespace and Rust is an explicit required
projection.

The plan now contains a synthetic-correctness step and release criterion with
closed-form, invariant, metamorphic, degeneracy, exhaustive, property, lineage,
cross-runtime, and mutation lanes. It also gates production work and goldens on
side-by-side exact-tag OCCT V8_0_0/V8_0_1 qualification. The Emscripten install
patch remains required. A canonical stdout fixture tests LF normalization,
final-newline removal, and the exact feasibility digest directly.

The compatibility snapshot now identifies `wavenumber-eng/appz` and the
`data_models` subtree. Publication of its frozen MATZ commits is pending the
MATZ-owned branch push requested through the agent bulletin; Geometer will not
publish or rewrite that sibling history.

After validation, the changed transport packet will receive new SHA-256 locks
and a new exact requested revision for focused re-review. Nothing in this log
authorizes transport, TypeSpec, packet, or production solver implementation.
