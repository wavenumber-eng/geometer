+++
type = "plan_log"
id = "geometer-typespec-contracts-2026-08-16T012734-0400"
plan_id = "geometer-typespec-contracts"
step_id = "filtered-analytic-solver-design"
created = "2026-08-16T01:27:34-04:00"
+++

# Log: filtered-analytic-solver-design

# Filtered arrangement review remediation

Closed the integration-review carrier-coverage defect in the indexed filtered arrangement. Carrier overlay now publishes the first active occurrence covering each emitted finite interval instead of the carrier group's arbitrary representative. The arrangement therefore accepts the production-shaped pair of lowered disks at `(0, 0)` and `(150, 0)` with radius `100 nm`, including its irrational circle-circle crossings, and closed contours with partially overlapping finite line domains. Every published span remains independently rebound to the named finite curve before topology construction.

Closed the performance-review late-preflight defect with an allocation-free integrated admission pass. The pass recognizes lowering's dense first-use carrier-token stream (and a proven strictly increasing prefix for hand-built internal fixtures), derives a safe minimum number of distinct noncollapsed spans, and combines their unavoidable arrangement memory/work with the owned overlay minimum. Jobs whose budget can admit overlay but cannot admit the known downstream arrangement now return before narrow/overlay evaluation with zero overlay predicates and zero overlay storage telemetry. Exact-count phase checks remain authoritative after overlay.

The endpoint-index coverage now distinguishes two cases. An integrated certified-geometry fixture proves the work ceiling propagates into reconciliation. A direct 128-entry adversarial fixture gives the exact shared AVL interval index overlapping, maximum-width enclosures that cannot merge, forces every earlier entry to be reported, and stops at the injected 4,096-visit ceiling. This covers the genuine dense-query path without weakening the finite-carrier validation required at the public arrangement boundary.

Canonical native/WASM evidence is byte-identical: filtered overlay remains 1,048 bytes and is repinned to SHA-256 `5cf09f630b60cf96a598e08cae394032d65d02a743359f8354f88600f775b3c1`; filtered arrangement is 11,352 bytes at SHA-256 `40b40f2b9579d342610b919880bfb6e1556438bb26f479a3cf307b9e78199584`.

Validation: native CTest 44/44 in both `build/` and `build-native-windows-x64/`; WASM CTest 43/43; all 23 native/WASM parity validators; Python 61/61 and manifest 15/15; native runtime/examples and clean-wheel package validation; TypeSpec, contract-doc, WASM, TypeScript, and Rust Rack strata; Ruff, Pyright, uv lock, clang-format, diff hygiene, and wn-dev-std repo/test/language/CI/compat audits. The arrangement source remains at or below CCN 15. Rack reports 88 passing and the same two plan-wide L99 failures as the accepted parent: request-packet validator CCN 113 exceeds 100, and the active temporary plan remains under `docs/plans`.

Next after exact-commit independent numeric, performance, and integration acceptance: indexed face ownership and ordered Boolean material classification.
